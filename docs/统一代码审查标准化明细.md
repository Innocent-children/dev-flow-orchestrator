# 四份 AI 代码审查报告标准化明细

> 基线：仓库提交 `0976fc88080c514dd72f9da4fe1016e638bc15bc`，报告文件为 `docs/Claude代码审查报告.md`、`docs/Codex代码审查报告.md`、`docs/DeepSeek代码审查报告.md`、`docs/Qwen代码审查报告.md`。  
> 本文件只执行第一阶段的字段标准化，不代表代码验证结论。字段内容均来自原报告；原报告没有明确给出的内容统一写为“未提供”。“问题分类”是编辑时使用的统一标签。  
> 计数口径：四份报告“问题清单”中的 59 个正式编号问题（Claude 22、Codex 13、DeepSeek 13、Qwen 11）。DeepSeek 的 P0 段仅是“需要验证”的低风险补充观察，编号为 DS00，但不计入 59 个正式问题。其他章节中的未编号判断作为比较或验证上下文，不另行计数。

## 1. Claude 报告（22 项）

### 1.1 来源、标题、分类、位置与原严重程度

| 原始 ID | 原报告来源 | 原问题标题 | 问题分类 | 文件路径 | 类、方法或代码位置 | 原报告严重程度 |
|---|---|---|---|---|---|---|
| CL01 | Claude代码审查报告 | 全量工作树 SHA-256 被反复重算，中大型仓库下 `review-snapshot` / `transition` 实质不可用 | 性能、正确性、并发 | `scripts/dev_flow.py` | `_tracked_worktree_manifest`、`_fingerprint_repo`、`_write_review_repo`、`_latest_passing_test_is_current`、`_review_is_current` | P1 |
| CL02 | Claude代码审查报告 | `hooks.json` 的 PreToolUse matcher 与 hook 支持的工具名不一致，`exec_command` / `shell` 守卫是死代码 | 安全性、配置 | `hooks/hooks.json`；`hooks/dev_flow_hook.py`；`scripts/validate_package.py`；`tests/test_hooks.py` | matcher；`handle`；validator matcher 断言；`test_unified_exec_cmd_alias_is_guarded` | P1 |
| CL03 | Claude代码审查报告 | Git remote URL 中的凭据未脱敏，原样写入 `state.json` 并打印到 stdout | 安全性、敏感信息 | `scripts/dev_flow.py` | `_remote_url`、preflight evidence、`command_show`、baseline fetch | P1 |
| CL04 | Claude代码审查报告 | 仓库内提交了陈旧的 Windows 交付物，污染 canonical 身份且与文档规则矛盾 | 配置与部署、发布完整性 | `tests/windows_test/*`；`scripts/candidate_identity.py`；`README.md`；`INSTALL.md` | canonical allowlist；Windows handoff 产物 | P1 |
| CL05 | Claude代码审查报告 | 对用户源仓库的 worktree/分支残留没有任何回收路径，claim 永久占用 | 生命周期、数据一致性 | `scripts/dev_flow.py`；`skills/follow-dev-flow/references/recovery.md` | `git worktree add` 路径；`_state_workspace_claims`；`RETIRED_WORKSPACE_REUSE`；`command_cancel` | P1 |
| CL06 | Claude代码审查报告 | `.cmd` 启动器以 LF 行尾发布，Windows 批处理行为需验证 | 兼容性、部署 | `.gitattributes`；`hooks/dev_flow_hook.cmd`；`scripts/windows_native_validation.cmd` | `FOR` 块内 `goto` 分支 | P2（需要验证） |
| CL07 | Claude代码审查报告 | 显式 `--data-dir` 时不会回退到脚本自身目录，外来 `PLUGIN_ROOT` 会使 hook 降级 | 配置、正确性 | `hooks/dev_flow_hook.py` | `_resolve_plugin_context` | P2 |
| CL08 | Claude代码审查报告 | `--data-dir` 允许相对路径，与“必须绝对”的文档不符 | 配置、数据一致性 | `hooks/dev_flow_hook.py`；`scripts/dev_flow.py` | `_resolve_plugin_context`；`resolve_data_dir` | P2 |
| CL09 | Claude代码审查报告 | mutation gate 对子进程 stderr 零容忍，任何 warning 变成需人工恢复的 quarantine | 异常处理、可用性 | `scripts/dev_flow.py` | mutation gate 协议检查及调用方 | P2 |
| CL10 | Claude代码审查报告 | `windows_native_validation.py` 在 `finally` 中 `return` | 异常处理、兼容性 | `scripts/windows_native_validation.py` | native validation 主流程的 `finally` | P2 |
| CL11 | Claude代码审查报告 | canonical allowlist 拒绝任意未知顶层目录，普通开发树无法运行发布校验 | 配置与部署、可维护性 | `scripts/candidate_identity.py`；`scripts/run_bundled_validators.py`；`README.md` | `_EXCLUDED_TOP_LEVEL` 与 snapshot validator | P2 |
| CL12 | Claude代码审查报告 | 捆绑注册与全局注册可能同时生效，文档没有去重指引 | 配置、性能 | `README.md`；`hooks/hooks.json`；用户级 hooks 配置 | Hook 注册与上下文生成 | P2（需要验证） |
| CL13 | Claude代码审查报告 | `check-ref-format` 在控制器 CWD 而非目标仓库中执行 | 正确性、兼容性 | `scripts/dev_flow.py` | `_run(["git", "check-ref-format", ...])` 的三处调用 | P2 |
| CL14 | Claude代码审查报告 | PreToolUse 的环境诊断返回了非法字段 | 框架契约、异常处理 | `hooks/dev_flow_hook.py`；`tests/test_hooks.py` | `_environment_diagnostic`；相关测试 | P2 |
| CL15 | Claude代码审查报告 | 原子写的固定开销与不可回收的临时文件 | 性能、资源清理 | `scripts/dev_flow.py` | `_atomic_write_bytes`；`recover-atomic-write` | P2 |
| CL16 | Claude代码审查报告 | 单文件 12812 行的控制器 | 架构设计、可维护性 | `scripts/dev_flow.py` | 整个控制器模块 | P3 |
| CL17 | Claude代码审查报告 | 文档版本与清单版本不一致 | 文档、配置与部署 | `.codex-plugin/plugin.json`；`INSTALL.md`；`README.md`；`README.zh-CN.md` | manifest version 与示例安装路径 | P3 |
| CL18 | Claude代码审查报告 | `hooks.json` 的 POSIX 命令硬编码 `python3` | 兼容性、部署 | `hooks/hooks.json` | 三个 POSIX Hook command | P3 |
| CL19 | Claude代码审查报告 | 每次 prompt 注入约 20 行上下文 | 性能、可维护性 | `hooks/dev_flow_hook.py`；`skills/follow-dev-flow/SKILL.md` | `build_context` | P3 |
| CL20 | Claude代码审查报告 | 死别名 | 可维护性 | `scripts/dev_flow.py` | `_WINDOWS_MUTATION_GATE_CODE`；`_windows_mutation_gate_command` | P3 |
| CL21 | Claude代码审查报告 | `load_active_task` 用异常做控制流 | 异常处理、可维护性 | `hooks/dev_flow_hook.py` | `load_active_task` | P3 |
| CL22 | Claude代码审查报告 | `_MUTATION_GATE_CODE` 是内联源码字符串 | 架构设计、测试与可维护性 | `scripts/dev_flow.py` | `_MUTATION_GATE_CODE` | P3 |

### 1.2 原始描述、原因、触发、影响、证据与建议

| 原始 ID | 问题描述 | 根本原因 | 触发条件 | 潜在影响 | 代码证据（原报告） | 修复建议（原报告） |
|---|---|---|---|---|---|---|
| CL01 | 指纹在 review snapshot 和终态转换中反复全树计算。 | 双观测 fail-closed 指纹每次逐文件 SHA-256，同一命令又多次调用。 | 40k 文件/2GB 仓库；实时杀毒、IDE 或 watcher 在观测间修改 tracked 内容。 | 报告称 full 流程可能极慢并因观测竞态无法完成。 | 列出 5 个函数位置，并估算单次 snapshot 六遍全树、终态转换四遍全树。 | 使用 Git 差异摘要并仅哈希脏文件；命令内缓存；保留廉价前后探针；变更证据契约并重跑相关测试。 |
| CL02 | matcher 只列 `Bash`，handler 还接受 `exec_command`/`shell`。 | matcher 与 handler 名单分别硬编码。 | Codex 使用 `exec_command` 或 `shell` 作为工具名。 | 报告称危险 Git 守卫会静默失效。 | 引用 matcher、handler、validator 和一个以 `Bash` 为 tool name 的测试。 | 扩展 matcher；让 validator 校验 handler 名单覆盖；增加真实 tool-name 别名测试。 |
| CL03 | credential-bearing remote URL 进入 state、stdout 和审批证据。 | 全链路无脱敏。 | remote 使用带 userinfo/token 或敏感 query 的 HTTPS URL。 | 凭据进入持久状态、模型上下文、会话或终端日志。 | 引用 `_remote_url`、preflight、`command_show`、fetch；给出 token URL 示例。 | 规范化脱敏并保留可比较身份；fetch 时重读实时 URL；更新证据契约与文档。 |
| CL04 | 旧 ZIP/JSON/run.txt 位于候选树内并被 canonical 纳入。 | handoff 输出提交到 candidate root，形成自指。 | release dispatch 使用旧摘要或以当前污染树重算摘要。 | 发布校验失败或错误发布；包膨胀；本机路径暴露。 | 原报告在 clean `git archive HEAD` 上比较：旧清单 42 路径/旧摘要，当前 45 路径/新摘要；旧 ZIP 不含自身目录。 | 移除/忽略 handoff 产物；candidate 内 fail-closed 拒绝；重算摘要与 cachebuster。 |
| CL05 | DONE/CANCELLED 后仍保留 worktree、分支和 claims，且无 release 命令。 | 生命周期只实现创建与冻结，未实现显式释放。 | 多个 full 任务完成/取消；手工清理后复用路径或分支；卸载并删除数据目录。 | 用户仓库残留、路径/分支永久不可复用、孤儿 worktree metadata。 | 引用 `worktree add`、claim 扫描、retired reuse、cancel 和 recovery 文档。 | 增加显式 `release-workspace`；以 `released_at` 而非终态过滤 claims；cancel 返回保留路径和释放建议。 |
| CL06 | 两个 `.cmd` 以 LF-only 发布。 | `.gitattributes` 强制批处理 LF。 | Windows cmd.exe 执行含 `FOR` 块内 `goto` 的 launcher。 | 报告认为 Hook 可能失效，但明确要求实机验证。 | 工作区字节检查与 `.gitattributes` 引用；未提供运行结果。 | Windows 分支强制测试；若失败改 CRLF 并加 validator。 |
| CL07 | 外来/陈旧 `PLUGIN_ROOT` 存在时，不使用脚本路径 fallback。 | `_resolve_plugin_context` 在 raw root 非空时完全跳过 fallback。 | 全局注册带 `--data-dir`，环境中另有无效 `PLUGIN_ROOT`。 | Hook 只给诊断，守卫与上下文注入降级。 | 引用 `_resolve_plugin_context` 分支。 | 当 supplied root 无 controller 时尝试可信 `fallback_root`，两者均失败才报错。 |
| CL08 | CLI 明示的相对 data-dir 被接受并按 CWD 解析。 | CLI 来源被绝对路径校验显式豁免。 | 全局 Hook 配置写 `--data-dir ".dev-flow"`。 | 不同仓库产生不同数据根，任务“消失”。 | 引用 CLI 豁免条件和 controller 的 `resolve(strict=False)`。 | 明示 `--data-dir` 必须绝对；相对值抛 `INVALID_ARGUMENT`。 |
| CL09 | mutation gate 把任何 stderr 当协议失败。 | 协议成功条件同时要求 return code 0、stderr 空、stdout 信封正确。 | Python/shim 输出非致命 warning 或 banner。 | 正常 Git mutation 被 quarantine，进入人工恢复。 | 引用 gate 条件和调用方；未提供实际复现。 | 仅以 return code 与 stdout 信封判断协议；stderr 脱敏后作为审计摘要。 |
| CL10 | `finally` 内 return 可覆盖传播中的异常。 | 控制流从 `finally` 返回。 | 运行中发生 `KeyboardInterrupt`/`SystemExit`；Python 3.14 发出 SyntaxWarning。 | 中断被伪装成普通失败；未来兼容性风险。 | 报告称 Python 3.14 测试输出 SyntaxWarning。 | 用标志记录报告写入失败，在 `finally` 后统一 return。 |
| CL11 | 严格 canonical allowlist 令含常见开发目录的工作树无法直接跑 snapshot validator。 | 未列入排除项的未知顶层路径全部拒绝。 | 根目录存在 `.agents`、`.venv`、`.vscode` 等。 | README 所列日常验证不可用，开发者可能跳过。 | 原报告在当前树运行并得到 `.agents` allowlist 错误。 | 保持发布身份严格；输出完整结构化诊断；支持干净导出/候选根；只排除确定非发布目录。 |
| CL12 | bundled 和 global Hook 可能同时执行。 | 回退文档未说明恢复 bundled 后移除 global，也无检测。 | 老版本配置 global，升级后 bundled 恢复。 | 重复上下文、双进程延迟、token 浪费；报告认为状态不会损坏。 | 文档与两个注册位置；未提供双注册运行结果。 | 文档明确互斥并给清理步骤；上下文加入实例标识以便识别。 |
| CL13 | `git check-ref-format` 未指定目标仓库。 | 三处调用遗漏 `-C repo`。 | 控制器 CWD 非 Git 仓库或位于另一仓库；使用依赖仓库上下文的边界 ref。 | 分支名合法性判定可能受错误仓库上下文影响。 | 引用三处 `_run` 调用。 | 改为 `git -C <repo> check-ref-format ...`。 |
| CL14 | 报告认为 PreToolUse 不允许 `additionalContext`。 | 报告按其理解将该字段归于 SessionStart/UserPromptSubmit。 | 环境变量缺失时进入 `_environment_diagnostic`。 | 报告称宿主丢弃诊断，配置错误不可见。 | 引用函数与只断言非 deny 的测试；未引用框架规范。 | PreToolUse 静默放行，把诊断留给其他事件，或使用其认为受支持的字段。 |
| CL15 | 原子写有多次哈希/fsync，并可能留下不被 recovery 识别的临时文件。 | 备份、校验、替换均采用完整读盘；SIGKILL 绕过临时文件清理。 | 高频 mutation；进程在 mkstemp 后被强杀。 | 小文件固定开销叠加；隐藏临时垃圾累积。 | 引用 `_atomic_write_bytes` 的步骤和临时文件命名差异。 | 复制时增量哈希；recovery 报告并可应用清理 orphan temporaries。 |
| CL16 | 一个控制器承载身份、锁、原子写、进程、Git 证据和所有命令。 | 多职责集中在单模块。 | 修改任意子系统。 | 导航和维护成本高。 | 文件行数与职责枚举。 | 按 identity/locking/atomicity/process/gitevidence/commands 拆包。 |
| CL17 | manifest 为 0.3.0，部分文档仍写 0.2.0。 | 版本信息多处手工维护。 | 未提供。 | 未提供。 | 引用 manifest、INSTALL、README 两种语言版本。 | validator 检查文档 base version 等于 manifest。 |
| CL18 | POSIX Hook 启动命令固定 `python3`。 | 未提供类似 Windows 的版本探测 shim。 | 系统只有 `python`/版本化解释器，或 `python3` 不在 GUI PATH。 | Hook 静默失效。 | 引用 `hooks/hooks.json`；未提供目标环境运行结果。 | 增加 POSIX launcher，探测受支持解释器。 |
| CL19 | 每次 prompt 注入较长的确认规则。 | 大段规则放在 checkpoint，而非仅在 Skill 激活时加载。 | 每次 SessionStart/UserPromptSubmit。 | token 与上下文占用。 | 约 20 行的人工估计；未提供 token 测量。 | checkpoint 只保留状态和命令前缀，其余移到 Skill。 |
| CL20 | Windows gate 常量和命令函数只是通用实现别名。 | 为测试保留的兼容别名缺乏清晰边界。 | 阅读或维护 gate 代码。 | 读者误以为存在 Windows 专用实现。 | 引用两个别名位置。 | 未提供。 |
| CL21 | 正常的不在 tasks 目录情形通过 `ValueError` + 总括捕获处理。 | 使用异常作为正常分支，并由宽泛捕获吞掉。 | cwd 不在 data-dir tasks 下。 | 结果正确，但可读性差且可能掩盖真实错误。 | 引用 `load_active_task`。 | 用 `_within()` 或局部 `ValueError` 处理。 |
| CL22 | mutation gate 代码以长内联字符串存在。 | 子进程代码未作为独立模块。 | 修改 gate、lint、类型检查或安全审计。 | 缺少语法高亮、lint 和直接覆盖。 | 引用 `_MUTATION_GATE_CODE`；未提供故障证据。 | 改为真实 `hooks/_gate.py`，用隔离 Python 执行并纳入 import audit。 |

## 2. Codex 报告（13 项）

### 2.1 来源、标题、分类、位置与原严重程度

| 原始 ID | 原报告来源 | 原问题标题 | 问题分类 | 文件路径 | 类、方法或代码位置 | 原报告严重程度 |
|---|---|---|---|---|---|---|
| CX01 | Codex代码审查报告 | 同一源码仓库可同时归属多个活动任务，Hook 任意选择“最新”任务 | 并发、权限边界、正确性 | `scripts/dev_flow.py`；`hooks/dev_flow_hook.py` | `find_active_task_for_cwd`；start 去重/task-id 检查；`load_active_task` | P1 |
| CX02 | Codex代码审查报告 | `state.json` 与 `events.jsonl` 不是原子事务，会产生半提交 | 事务和数据一致性、异常处理 | `scripts/dev_flow.py` | `_append_event`；`_commit_state` | P1 |
| CX03 | Codex代码审查报告 | 凭据化 remote URL 和完整命令被写入状态、隔离文件及错误输出 | 安全性、敏感信息 | `scripts/dev_flow.py` | mutation intent；`_remote_url`；preflight/baseline；`_run` 错误详情 | P1 |
| CX04 | Codex代码审查报告 | 文档鼓励全局 Hook 回退，但没有防止插件 Hook 与全局 Hook 同时加载 | 配置、性能、正确性 | `README.md`；`hooks/hooks.json` | Hook 安装/回退说明 | P1 |
| CX05 | Codex代码审查报告 | Windows Hook 启动器因 `call` 二次解析 `%PLUGIN_ROOT%` | 兼容性、安全性 | `hooks/dev_flow_hook.cmd`；`hooks/hooks.json`；`README.md` | Python 探测和实际启动行 | P2 |
| CX06 | Codex代码审查报告 | Unix Hook 固定使用 `python3`，与声明的 3.9–3.14 契约不一致 | 兼容性、部署 | `hooks/hooks.json` | 三个 POSIX command | P2 |
| CX07 | Codex代码审查报告 | 每次 Hook 都全量扫描历史任务，固定 10 秒且失败时放行 | 性能、并发 | `scripts/dev_flow.py`；`hooks/dev_flow_hook.py`；`hooks/hooks.json`；`README.md` | `find_active_task_for_cwd`；Hook 调用和 timeout | P2 |
| CX08 | Codex代码审查报告 | 发布候选不是封闭清单，并已包含陈旧交接产物 | 配置与部署、发布完整性 | `scripts/candidate_identity.py`；`scripts/validate_package.py`；`tests/windows_test/*`；`INSTALL.md` | allowlist 与 package validator inventory | P2 |
| CX09 | Codex代码审查报告 | 取消和卸载没有处理外部状态、linked worktree 与全局配置 | 生命周期、资源清理 | `scripts/dev_flow.py`；`INSTALL.md` | `command_cancel`；安装/升级说明 | P2 |
| CX10 | Codex代码审查报告 | 跨平台支持声明先于项目自己的发布证据闭环 | 测试、配置与部署 | `README.md`；`openspec/changes/complete-cross-platform-support/tasks.md` | 支持声明、发布前提、未完成任务 | P2 |
| CX11 | Codex代码审查报告 | CI 的部分供应链依赖仍使用可变标签或无哈希安装 | 安全性、配置与部署 | `.github/workflows/cross-platform.yml` | Action、pip、npm 安装步骤 | P2 |
| CX12 | Codex代码审查报告 | 候选发布依赖硬链接，不支持部分合法文件系统 | 兼容性、部署 | `scripts/candidate_identity.py` | exclusive-create 的 `os.link` | P3 |
| CX13 | Codex代码审查报告 | 版本和进度文档陈旧 | 文档、配置与部署 | `.codex-plugin/plugin.json`；`INSTALL.md`；`README.md`；OpenSpec `tasks.md` | version/cachebuster 与任务清单 | P3 |

### 2.2 原始描述、原因、触发、影响、证据与建议

| 原始 ID | 问题描述 | 根本原因 | 触发条件 | 潜在影响 | 代码证据（原报告） | 修复建议（原报告） |
|---|---|---|---|---|---|---|
| CX01 | start 允许不同 task-id 先后或并发占用同一源码仓库，Hook 从多个匹配中取“最新”。 | 只做单次请求内 common-dir 去重和 task-id 冲突检查，没有跨任务 repository claim。 | 同一仓库启动两个 full/lite/in-place/branch 任务，尤其并发启动。 | Hook 可能使用错误任务的写权限、分支策略、检查点与审批。 | 列出 start/lookup/hook 位置；原报告标为“确定存在”。 | 在全局锁内维护 canonical root/filesystem/common-dir claim；明确共享/互斥规则；Hook 多匹配时拒绝而非任选。 |
| CX02 | state 先替换，event 后追加，event 失败时形成 revision 缺口。 | 两个持久对象不在同一可恢复事务中；无 outbox；`os.write` 未处理短写。 | 磁盘满、ACL/杀毒占用、进程终止、事件文件损坏。 | CLI 报失败但状态已前进；重试冲突；审计链不完整。 | 原报告称用 `_append_event` 故障注入确认 revision 1→2 但事件缺失。 | durable outbox/journal；按 event_id 幂等刷入 JSONL；加载/下次 mutation 恢复；循环处理短写。 |
| CX03 | remote、完整 mutation command 和 raw stderr 可进入状态、隔离和输出。 | 无统一 secret scrubber，持久化原始 URL/命令/错误详情。 | URL 含 token/userinfo/query；命令含 token/header。 | 凭据进入 state、quarantine、CLI JSON、终端日志、备份或支持包。 | 引用 7 处代码路径；原报告标为“确定存在”。 | 保存脱敏 URL + 完整值摘要；fetch 重读实时值；quarantine 结构化脱敏；统一清洗 stderr/错误详情。 |
| CX04 | 文档设置 global fallback，但未处理 bundled 恢复后的双注册。 | 无互斥说明、清理步骤或重复 handler 检测。 | 信任/缓存/版本变化使 bundled 恢复，而 global 仍在。 | 重复上下文与延迟；不同版本可给出冲突策略，任一 deny 可阻断。 | 引用 README 和 bundled hooks；原报告标为“确定存在”。 | 文档明确二选一；增加 doctor/安装检查；回退文档加入移除步骤；升级测试覆盖双版本。 |
| CX05 | `call` 对展开后的插件路径再次执行 `%...%` 变量替换。 | 对 `.exe` launcher 使用 `call`。 | 插件目录名包含 `%NAME%` 且环境定义该变量。 | launcher 找错脚本，Hook fail-open 消失。 | 原报告称以 `plugin-%DEV_FLOW_TEST_TOKEN%` 实测被改为 `plugin-expanded`。 | 不对 `.exe` 使用 `call`；先定位/验证 executable 再直接调用；增加特殊路径测试。 |
| CX06 | POSIX 直接调用 PATH 上的 `python3`，没有版本范围探测。 | Unix 配置和 Windows shim 契约不一致。 | `python3` 为 3.8/3.15、缺失，或 GUI PATH 不含它但存在受支持版本。 | bundled Hook 启动失败或运行于未支持解释器。 | 引用三条 hook command；未提供目标系统运行复现。 | 提供 POSIX launcher，验证/枚举 3.14–3.9并给可见诊断。 |
| CX07 | 每个 Hook 新进程全量读取 `tasks/*/state.json` 并进行路径身份判断。 | 无持久活动任务索引；进程缓存不能跨 Hook 复用。 | 数千任务、离线 UNC/网络路径、杀毒或路径解析变慢。 | 超过 10 秒后上下文与 guardrail 同时 fail-open。 | 引用 lookup、调用路径、timeout；明确“潜在高风险，需要规模验证”。 | 原子维护 cwd/common-dir identity 索引；终态清理；全扫用于重建；增加 10/1000/10000 任务基准。 |
| CX08 | 目录级 allowlist 接受额外嵌套文件，当前树已含旧 handoff；两个 validator 对开发树的语义不一致。 | release inventory 不精确且不共享。 | 额外 debug/secret/旧 ZIP 被提交；当前开发根含 `.agents`。 | 发布包污染、自指摘要、验证结果相互矛盾。 | 报告给出 45 vs 42 路径和旧摘要，并称当前 `.agents` 被 snapshot 拒绝但 package validator 通过。 | 共享精确 release inventory；移除旧产物；额外项统一 fail-closed；明确 `.agents` 规则。 |
| CX09 | cancel 仅改状态，安装文档也没有卸载/清理工作流。 | 没有安全、显式、可审计的外部资源释放设计。 | 取消 full task、卸载、移动安装目录、停止 global fallback。 | worktree/branch/registry/data-dir/global Hook 残留。 | 引用 `command_cancel` 和安装文档；原报告标为“确定存在”。 | 增加只读 uninstall-plan/doctor；删除需单独授权并验证 ownership、common-dir、干净状态。 |
| CX10 | README 已称三平台受支持，但同文档和 OpenSpec 仍要求/缺少真实 native 证据。 | 对外支持状态和发布证据清单未同步。 | 用户按开头声明部署到 Windows/UNC/特殊路径。 | 当前仓库不足以证明同摘要全矩阵和真实 host discovery。 | 引用 README 支持/发布段与 OpenSpec 未勾项。 | 证据闭环前改为“目标支持/验证中”；完成同摘要 native CI/Windows report/host smoke 后恢复。 |
| CX11 | CI 仍依赖可变 major action 标签及无 hash/lock 的包安装。 | 依赖完整性策略只覆盖了 Codex validator，没有统一覆盖工作流。 | 上游标签移动、发布账户或依赖供应链受损。 | 发布验证环境可被替换。 | 列出 `checkout@v4`、`setup-python@v5`、`setup-node@v4`、PyYAML 与 npm 安装。 | Actions 固定完整 SHA；Python requirements 带 hash；npm 使用 lockfile/integrity。 |
| CX12 | exclusive-create 通过硬链接临时文件到目标。 | `os.link` 被当作不覆盖原子发布的唯一手段。 | FAT/exFAT、禁硬链接 SMB、容器/企业挂载。 | 目录可写但 handoff 仍失败。 | 引用 `candidate_identity.py` 两处实现；未提供目标文件系统复现。 | 提供安全 no-clobber 回退，或 preflight 检测能力并给可操作错误；增加模拟。 |
| CX13 | manifest 0.3.0 与文档 0.2.0、OpenSpec 进度不一致。 | 多源手工维护且无一致性验证。 | 升级 marketplace 或执行发布清单。 | 缓存预期错误，审核无法区分未实现与未验收。 | 引用 manifest、文档和任务清单。 | 以 manifest 为单一来源生成/验证版本；增加一致性测试。 |

## 3. DeepSeek 报告（13 项，另有 1 条非正式观察）

### 3.1 来源、标题、分类、位置与原严重程度

| 原始 ID | 原报告来源 | 原问题标题 | 问题分类 | 文件路径 | 类、方法或代码位置 | 原报告严重程度 |
|---|---|---|---|---|---|---|
| DS00 | DeepSeek代码审查报告 | mutation gate 继承“被污染的 Python 解释器”的待验证观察 | 安全性、运行时信任 | `scripts/dev_flow.py` | `_MUTATION_GATE_CODE` 的 `sys.executable -I -S -c` 执行链 | 非正式观察；P0 段明确称风险极低、无确定 P0 |
| DS01 | DeepSeek代码审查报告 | Hook 与 Controller 之间存在大量重复的常量定义 | 架构设计、可维护性、正确性 | `hooks/dev_flow_hook.py`；`scripts/dev_flow.py` | 模块级状态机、名称、next-action、gate 常量 | P1 |
| DS02 | DeepSeek代码审查报告 | Hook 导入 12,813 行的 Controller 仅用于 scope 解析，影响启动性能 | 性能、架构设计 | `hooks/dev_flow_hook.py`；`scripts/dev_flow.py` | `_import_controller`；`in_configured_scope`；`load_active_task` | P1 |
| DS03 | DeepSeek代码审查报告 | `find_active_task_for_cwd` 在大量任务时的 O(n) 扫描可能超时 | 性能 | `scripts/dev_flow.py` | `find_active_task_for_cwd` | P1 |
| DS04 | DeepSeek代码审查报告 | `_same_path` 对不存在的路径使用 fallback 比较可能产生误判 | 正确性、路径身份 | `scripts/dev_flow.py` | `_same_path`、`_filesystem_identity`、`_nearest_existing_path` | P1 |
| DS05 | DeepSeek代码审查报告 | `_raw_cmd_prefix_payload` 对外层引号的启发式剥离可能误剥离 | 安全性、命令解析 | `hooks/dev_flow_hook.py` | `_raw_cmd_prefix_payload` | P2 |
| DS06 | DeepSeek代码审查报告 | `_write_targets` 从 patch 内容提取路径的可靠性有限 | 安全性、权限边界 | `hooks/dev_flow_hook.py` | `_PATCH_PATH`；`_write_targets`；`_write_denial_reason` | P2 |
| DS07 | DeepSeek代码审查报告 | Windows `dev_flow_hook.cmd` 的 Python 版本探测范围有限且诊断消息可能过时 | 兼容性、部署 | `hooks/dev_flow_hook.cmd` | Python launcher 探测 | P2 |
| DS08 | DeepSeek代码审查报告 | `build_bootstrap_context` 和 `build_context` 硬编码中文指令，不可定制 | 配置、国际化 | `hooks/dev_flow_hook.py` | `build_bootstrap_context`；`build_context` | P2 |
| DS09 | DeepSeek代码审查报告 | `_cmd_segments` 在非严格模式下不检测 `%VAR%` 和 `!VAR!` 动态扩展 | 安全性、命令解析 | `hooks/dev_flow_hook.py` | `_cmd_segments`；`_payload_has_dynamic_expansion`；调用链 | P2 |
| DS10 | DeepSeek代码审查报告 | 探针目录在进程被 SIGKILL 时可能残留 | 资源清理 | `scripts/dev_flow.py` | 两个 filesystem probe 函数 | P3 |
| DS11 | DeepSeek代码审查报告 | 锁文件残留 | 资源清理、可维护性 | `scripts/dev_flow.py` | `_file_lock` | P3 |
| DS12 | DeepSeek代码审查报告 | `_render` 函数 500 字符截断可能丢失信息 | 可观测性、用户体验 | `hooks/dev_flow_hook.py` | `_render` | P3 |
| DS13 | DeepSeek代码审查报告 | Skill 文件内容较简略 | 文档、AI 工作流设计 | `skills/analyze-change-impact/SKILL.md`；`skills/review-dev-flow-change/SKILL.md` | 整体文件 | P3 |

### 3.2 原始描述、原因、触发、影响、证据与建议

| 原始 ID | 问题描述 | 根本原因 | 触发条件 | 潜在影响 | 代码证据（原报告） | 修复建议（原报告） |
|---|---|---|---|---|---|---|
| DS00 | gate 用启动 controller 的同一 `sys.executable` 启动隔离子进程。 | 调用方环境可能影响解释器路径。 | Hook 本身已由被污染解释器启动。 | 原报告称风险极低；未给出具体增量影响。 | 指出 command 为 argv list、无 shell 注入，并使用 `-I -S`。 | 未提供。 |
| DS01 | Hook 复制 controller 的状态、中文名、下一动作和 gate 定义。 | 少数路径导入 controller，其余使用本地副本。 | 修改状态机或显示名称后漏同步 Hook。 | 注入错误上下文；controller 最终校验可避免状态损坏，但体验和效率下降。 | 列出十组重复常量。 | 抽取轻量 `scripts/constants.py`，两端共同导入并保留必要 re-export。 |
| DS02 | Hook 为 scope/任务查找导入完整 controller。 | 相关函数与依赖未拆为轻量模块。 | 每个事件新 Hook 进程；低配或网络文件系统。 | 启动延迟；`sys.path.insert` 产生进程级副作用。 | 引用 `_import_controller` 和四个 imported functions；未提供计时。 | 拆 `scope.py`、`task_lookup.py` 等轻量模块。 |
| DS03 | 每次任务查找读取并反序列化全部状态。 | 无活动任务索引；终态过滤也要先读文件。 | 积累数百历史任务并频繁触发 Hook。 | timeout 后 fail-open，上下文状态“消失”。 | 引用 glob、load、排序，并注明已有终态过滤。 | 维护原子索引；考虑提前退出；设置总时间限制。 |
| DS04 | 报告怀疑不存在路径的祖先身份 fallback 可误判。 | 目标不存在，身份由最近已存在祖先推导。 | 多挂载/bind mount 且两个未创建路径共享祖先。 | scope、preflight、capability profile 路径匹配误判。 | 同时承认代码还比较 normalized、suffix parts、anchor/parts，明确“需要验证”。 | 不存在路径至少要求 normalized 完全相等；增加跨文件系统测试。 |
| DS05 | cmd payload 首尾引号会无条件剥一层。 | 以字符启发式替代完整 cmd `/s /c` 引号规则。 | 单一、含空格、带引号的 executable-only payload。 | 分词错误后可能漏识别 Git 子命令。 | 引用剥引号条件并给出 `cmd /c ""some tool.exe""` 示例。 | 更精确模拟 cmd；根据剥离后首 token 是否仍受引号保护决定。 |
| DS06 | patch 目标提取仅识别四种 `*** ...` header。 | 单一正则、有限 patch 格式。 | AI 产生 unified diff/`diff --git` 或正文含相似标记。 | targets 空时只看 workdir，报告认为可能漏掉越界写入；同时承认 `..` 会被 resolve。 | 引用 `_PATCH_PATH`、targets 回退和 `_path`。 | 支持更多格式；无法解析 target 时要求确认。 |
| DS07 | Windows 版本范围和文案硬编码 3.9–3.14。 | 显式版本列表需人工更新。 | 未来只有 3.15、无 `py`、也无有效 `python`。 | Hook 静默退出。 | 列出 `py -3`、3.14…3.9、`python` 顺序。 | 动态/循环探测；文案改为 3.9+；增加找不到解释器诊断。 |
| DS08 | 上下文强制中文且不可配置。 | 文本直接硬编码。 | 非中文用户或英文项目。 | AI 输出语言不符合偏好。 | 引用两组中文指令。 | 按 locale 或 `config.json.locale` 选择语言。 |
| DS09 | 非严格 cmd tokenizer 不检查 `%VAR%`/`!VAR!`。 | 动态扩展检测只在 strict 调用。 | 命令使用 cmd 环境变量构造路径/命令。 | 可能漏识别扩展后的 Git；报告也称双解析通常可捕获。 | 引用 strict 分支和 tokenizer。 | 在 `_cmd_segments` 检测动态标记。 |
| DS10 | SIGKILL 可绕过 probe 的 finally。 | 临时目录清理依赖正常解释器退出。 | 首次探针期间被强杀。 | 留下隐藏目录；报告明确称不影响功能且影响极小。 | 引用两个 probe 的 finally 和缓存行为。 | 未提供。 |
| DS11 | OS 释放锁后 1 字节 lock 文件仍存在。 | 代码不删除锁文件。 | 正常使用或崩溃。 | 仅目录观感混乱；原报告明确称无功能影响。 | 列出四类 lock 文件和 reopen 行为。 | 考虑在 `_file_lock` finally 中删除。 |
| DS12 | `_render` 固定截断到 500 字符。 | 固定长度上限。 | 路由描述、审批备注超过 500 字符。 | 可能丢失关键信息。 | 只引用函数行号；未提供实际样例。 | 未提供。 |
| DS13 | 两个 Skill 比 follow-dev-flow 简短。 | 报告认为其更依赖 AI 推理。 | 未提供。 | 未提供。 | 报告给出 39/52 行并作长度对比。 | 未提供。 |

## 4. Qwen 报告（11 项）

### 4.1 来源、标题、分类、位置与原严重程度

| 原始 ID | 原报告来源 | 原问题标题 | 问题分类 | 文件路径 | 类、方法或代码位置 | 原报告严重程度 |
|---|---|---|---|---|---|---|
| QW01 | Qwen代码审查报告 | Hook 与全局注册同时存在时可能导致重复上下文注入 | 配置、性能 | `hooks/hooks.json`；`README.md` | bundled/global 注册；`handle` | P1 |
| QW02 | Qwen代码审查报告 | `find_active_task_for_cwd` 在大量任务时存在性能隐患 | 性能 | `scripts/dev_flow.py` | `find_active_task_for_cwd` | P1 |
| QW03 | Qwen代码审查报告 | `_current_branch` 的 subprocess 调用在 Hook 热路径上 | 性能、可用性 | `hooks/dev_flow_hook.py` | `_current_branch` | P1 |
| QW04 | Qwen代码审查报告 | `_quote` 函数在 Windows 上不处理 `%` 和 `!` 字符 | 兼容性、安全性 | `hooks/dev_flow_hook.py` | `_quote` | P2 |
| QW05 | Qwen代码审查报告 | Hook 的 `main()` 对所有异常静默返回 0 | 异常处理、可观测性 | `hooks/dev_flow_hook.py` | `main` | P2 |
| QW06 | Qwen代码审查报告 | `_posix_segments` 使用 `shlex` 的 `punctuation_chars` 模式可能误分割 | 安全性、命令解析 | `hooks/dev_flow_hook.py` | `_posix_segments` | P2 |
| QW07 | Qwen代码审查报告 | `load_active_task` 中的 `cwd.relative_to(data_dir / "tasks")` 异常路径 | 异常处理、性能 | `hooks/dev_flow_hook.py` | `load_active_task` | P2 |
| QW08 | Qwen代码审查报告 | 控制器 12800+ 行单文件 | 架构设计、可维护性 | `scripts/dev_flow.py` | 整个控制器 | P2 |
| QW09 | Qwen代码审查报告 | `plugin.json` 中缺少 `hooks` 字段是有意设计但可能引起困惑 | 配置、文档 | `.codex-plugin/plugin.json`；`INSTALL.md` | manifest 与默认 Hook discovery | P3 |
| QW10 | Qwen代码审查报告 | `dev_flow_hook.cmd` 中 `py -3` 版本检查执行了两次 Python | 性能、兼容性 | `hooks/dev_flow_hook.cmd` | 版本检查与 `run_py` | P3 |
| QW11 | Qwen代码审查报告 | 测试中硬编码的日期字符串 | 测试、代码风格 | `tests/test_hooks.py` | 测试 fixture 默认 `updated_at` | P3 |

### 4.2 原始描述、原因、触发、影响、证据与建议

| 原始 ID | 问题描述 | 根本原因 | 触发条件 | 潜在影响 | 代码证据（原报告） | 修复建议（原报告） |
|---|---|---|---|---|---|---|
| QW01 | bundled/global 两份相同 Hook 会同时处理事件。 | 回退注册与默认发现可共存。 | 先加 global，升级/缓存恢复后 bundled 生效。 | 重复上下文、token 和延迟；报告明确称不会损坏数据。 | 引用 `hooks.json` 和 README step 3；未提供实际双注册运行。 | handler 幂等标记或文档明确 bundled 工作时删除 global。 |
| QW02 | 每个 Hook 遍历所有 `tasks/*/state.json` 并做路径检查。 | 无持久活动索引。 | 50+ 任务，HDD/网络文件系统。 | 逼近 10 秒 timeout，Hook fail-open。 | 引用 lookup 行号；未提供基准。 | active-tasks 索引、最近 N 个过滤、mtime/目录预筛。 |
| QW03 | 遇到 commit/push 时会启动 Git 子进程查询分支。 | 策略需要实时分支信息。 | 大仓库或 Git index 冷，`git branch --show-current` 变慢。 | 最多占用 2 秒，叠加 Hook timeout 后检查可能失效。 | 引用 `_current_branch` 和 2 秒 timeout；未提供计时。 | timeout 降到 1 秒，超时用 task state fallback。 |
| QW04 | Windows 显示 quoting 不转义 `%VAR%`/`!VAR!`。 | `_quote` 只处理部分 cmd 元字符。 | controller/data-dir 字面路径含 `%PATH%`/`!test!`。 | 报告称 cmd 可能展开并指向错误目标，同时承认输出主要是给 AI 看的文本。 | 引用 `_quote`；未提供真实执行。 | `%` 变 `%%`，或记录为已知限制。 |
| QW05 | 任何内部异常都被总括捕获并返回 0。 | fail-open 设计没有诊断通道。 | 解析错误或重构引入 bug。 | guardrail 可长期静默失效。 | 引用 `main` 的 `except Exception: return 0`。 | stderr 或 `<PLUGIN_DATA>/hook-errors.log` 记录错误，stdout 保持协议干净。 |
| QW06 | 重定向 token 被当成命令段分隔符。 | `punctuation_chars` 合并标点，代码把全分隔字符 token 视为 separator。 | `git log >> output.txt` 等重定向。 | 原报告认为影响有限、总体保守，但可能漏检某些 Git 调用。 | 给出 `>>` 分割示例；未提供实际危险命令绕过。 | 标记已知限制，或保留重定向语义。 |
| QW07 | 非 tasks cwd 通过抛/捕 `ValueError` 返回 None。 | 正常路径使用宽泛异常控制流。 | 大多数 cwd 不在 data-dir tasks 下。 | 无功能影响，轻微异常开销和可读性问题。 | 引用 `relative_to` 和外层捕获。 | 局部捕获或先 `_within`。 |
| QW08 | controller 集中所有子系统。 | 单文件插件部署与职责拆分之间的设计取舍。 | 未提供。 | 导航和维护成本。 | 文件行数和职责概述；无故障证据。 | 原报告倾向保留单文件，仅增加清晰 section 注释。 |
| QW09 | manifest 未显式声明 hooks。 | 依赖 `hooks/hooks.json` 默认发现，且 validator 有意拒绝 manifest hooks。 | 未提供。 | 仅可能困惑。 | 原报告明确 README/测试已验证此设计。 | 无需修改；INSTALL 已说明。 |
| QW10 | `py -3` 先探测后再启动脚本，启动 Python 两次。 | 探测与执行分为两个进程。 | 每次 Windows Hook。 | 约 50–100ms，报告认为在 10 秒内可忽略。 | 引用 launcher 两条调用；计时为估计。 | 可接受；若优化让实际脚本自检。 |
| QW11 | fixture 日期固定为 2026-07-21。 | 测试数据硬编码。 | 未提供。 | 原报告明确称无功能影响、仅风格。 | 引用 `test_hooks.py` 行 89。 | 无需修改。 |

## 5. 标准化计数核对

| 报告 | P0 | P1 | P2 | P3 | 正式问题数 |
|---|---:|---:|---:|---:|---:|
| Claude | 0 | 5 | 10 | 7 | 22 |
| Codex | 0 | 4 | 7 | 2 | 13 |
| DeepSeek | 0 | 4 | 5 | 4 | 13 |
| Qwen | 0 | 3 | 5 | 3 | 11 |
| 合计 | 0 | 16 | 27 | 16 | 59 |

DeepSeek 的 DS00 不计入 P0 或正式问题总数，因为原报告明确写的是“无确定的 P0”，且该段只提出低风险待验证观察。
