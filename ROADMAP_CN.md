# Dev Flow Orchestrator 产品路线图

## 当前产品：0.3.0 任务范围自适应保障

当前产品已交付任务自有变更胶囊、成员租约、索引精确 Git 证据、封闭的六档保障策略、
确定性义务调度、绝对尝试预算、切片感知证据复用、结构化因果审查和可解释的 Dossier
收尾。它取代了此前的固定循环：经源码确认的聚焦变更只运行推导出的检查，封闭风险触发器
和未知影响则保守扩展保障。历史 0.2 状态保留在 0.3 命名空间之外，不属于运行时兼容面。

阶段 2 的首个展示切片也已在同一个 0.3.0 产品中交付：一个经过认证、仅监听 loopback
的本地只读 Web UI，提供可搜索的持久化清单、有界任务详情和时间线、Delivery Dossier
摘要、下一步原因/恢复指引，以及对选中任务的显式实时观察。阶段 2 的其余部分并未交付：
任务状态变更、审批、工作流编辑、工件浏览、可复用工作流管理和外部服务仍处于规划中。

[English](ROADMAP.md)

**状态：** 持续演进的功能路线图

**规划模型：** 按产品依赖关系编排、以成果证据推进的能力阶段

**最近审阅：** 2026-08-03

## 北极星产品愿景

Dev Flow Orchestrator 应成为 AI 辅助软件开发的本地优先交付控制平面。
开发者从一项需求、Issue 或规格出发；产品将其转化为持久、可解释的交付计划，
协调 Codex 与周边开发工具链，最终形成可合并、可发布或可移交的变更。

产品应掌管持久化的协调模型：

- 交付契约与验收标准；
- 任务图与当前执行状态；
- 仓库与工作区计划；
- 人工决策、批准与例外；
- 工件、证据、来源与新鲜度；
- 最终的 Delivery Dossier。

Codex 负责代码编写与审查，Git 掌管源码历史。OpenSpec、codebase-memory、CI、
Issue 跟踪器和 Git 托管平台提供各自的专业能力。Dev Flow Orchestrator 负责协调
它们，让意图、工作、决策和证据能够跨会话延续，并最终汇聚为一个交付成果。

### 终局用户旅程

```text
需求 / Issue / 规格
  → 交付契约与验收标准
  → 影响分析与任务拆解
  → 仓库、工作区与执行计划
  → 人工决策与并行 Codex 工作
  → 验证、独立审查与集成
  → Delivery Dossier、PR 移交或发布移交
```

在这一终局形态下，开发者专注于真正需要判断的决策：范围、设计、风险、例外与
最终批准。编排器持续承载完整的交付上下文，并推进常规协调工作。

## 当前产品：0.3.0 完整个人交付

阶段 1 已在 0.3.0 中交付。开发者可以通过一个可恢复的本地任务，把功能、缺陷修复、
调查、重构或快速变更从结构化意图推进为当前有效的 Delivery Dossier。已发布产品提供：

- `lite`、`feature`、`bugfix`、`investigation`、`refactor` 和 `full`
  工作流，以及以绝对路径固定的 `dev-flow-workflow/0.3.0` 定义；
- 带版本的交付契约、追加式修订、决策、验收标准豁免和精确的审查保障豁免；
- 带契约绑定、生产者、安全仓库快照、输入血缘、治理资源、摘要值和派生新鲜度的
  类型化工件；
- 一个由 1–8 个 `user-prepared`（用户预备）本地 Git 工作树组成的精确规范集合，
  以及聚合快照、范围化资源、验证、审查、新鲜度和 Dossier 证据；
- 可选的 OpenSpec、codebase-memory 和独立审查阶段，并显式记录降级或不可用结果；
- 有限的验证与审查返工，以及成功或未完成的 Dossier 收尾；
- CLI、Hook 与 Skills 共享的单一控制器权威，并由锁、修订 CAS、确定性重放和
  原子替换提供支撑。

当前 0.3.0 核心支持一个任务、一个不可变仓库集合、一个当前动作和 `one Codex`
（一个 Codex 执行者）。所有集合基数统一使用 `dev-flow-agent/0.3.0`、聚合仓库集合
快照、按仓库范围化资源、结构化逐成员/集成验证和 Delivery Dossier 0.3.0。
当前核心不管理 branch/worktree，不发布 Git 变更，不协调并行 Agent，不操作
`external CI`、PR 或发布系统。对于能够证明未变化且与后续改动不相交的任务自有切片，
当前核心会对仍然有效的证据进行部分复用，并按切片选择性保留。

## 相互独立的产品维度

能力模型将每个维度视为可独立选择的产品变量。工作流深度、仓库拓扑、执行拓扑与
工作区策略通过一份明确的受支持能力矩阵组合。Git branch 与工作流分支保持为不同概念。

| 维度 | 0.3.0 当前选项 | 后续阶段选项 |
|---|---|---|
| 工作流深度 | 六个官方个人工作流和固定的 `dev-flow-workflow/0.3.0` 自定义定义 | 可复用工作流包和更多交付工作流族 |
| 控制流 | 一个当前动作，以及带绝对预算的自适应保障义务 | 通用命名结果、可选阶段、扇出与汇聚 |
| 任务拓扑 | 一个任务 | 父子任务图、项目级交付项与批次 |
| 仓库拓扑 | 1–8 个本地工作树的一个精确规范集合 | monorepo 组件范围、仓库角色与依赖拓扑 |
| 工作区策略 | 传入的当前工作树 | 带所有权的现有或托管 branch/worktree 策略 |
| 执行拓扑 | 一个 Codex 执行者 | 角色分离与并行执行者 |
| 协作模式 | 本地个人交付 | 可验证移交与团队共享协调 |
| 保障级别 | 验收覆盖、聚焦逐成员与集成义务、因风险触发的独立审查、切片感知复用与显式豁免 | 外部 CI、发布门禁与跨系统证据复用 |

一份权威的能力矩阵应统一驱动运行时验证、工作流选择、UI 可用性、测试覆盖、
打包与文档，并明确给出所有受支持组合与产品限制。

## 能力目标图谱

| 能力领域 | 0.3.0 产品 | 目标形态 | 用户价值 |
|---|---|---|---|
| 意图与范围 | 带版本的契约、稳定验收标准、修订、决策与豁免 | 策略约束的批准与委派范围权限 | 每项实施与证明都能追溯到已接受意图 |
| 工作流组合 | 六个官方个人工作流与固定的 `dev-flow-workflow/0.3.0` 自定义定义 | 可复用工作流包和更多风险/领域工作流族 | 用户选择与工作匹配的交付严谨度 |
| 执行模型 | 一个任务、一个当前动作、一个执行者 | 依赖图、认领、租约、扇出、汇聚与操作员干预 | 复杂工作可以拆分、恢复并重新汇合 |
| 仓库与工作区 | 1–8 个传入当前工作树的一个精确集合，成员关系不可变并采用聚合恢复 | 组件范围、角色、依赖和带所有权的 branch/worktree 策略 | 大型变更具备明确范围与隔离 |
| 决策与权限 | 绑定契约的验收标准/审查保障豁免及操作主体标签 | 经过身份验证的角色、法定人数、批准与精确 effect 授权 | 判断可归责且范围狭窄 |
| 保障与证据 | 类型化血缘、新鲜度、验收覆盖、有界审查/验证与 Delivery Dossier | 横跨 CI、集成和发布工件的持续保障 | “完成”由当前证明或显式例外支撑 |
| 产品体验 | CLI、Hook 接取、单动作投影与完整任务视图 | 可搜索驾驶舱、时间线、工件浏览器、批准收件箱与下一步原因说明 | 用户通过受支持视图理解进展与阻塞 |
| 协作与生态 | 带声明式可选 driver 的本地个人任务 | 可验证移交、团队角色、策略包、可信注册中心与范围受控连接器 | 经验证的交付方法可跨团队复用 |

## 功能阶段总览

各阶段依照产品依赖关系排列。当前支持范围为阶段 1，即 0.3.0 已交付能力；阶段 2–7
是规划中的产品方向。

| 阶段 | 状态 | 用户成果 | 依赖 |
|---|---|---|---|
| 1. 完整的个人交付 | 已在 0.3.0 交付 | 将真实变更从意图推进为证据完备的 Delivery Dossier | Dev Flow 0.3.0 本地控制器与 ledger |
| 2. 交互式工作流工作台 | 规划中 | 从一个驾驶舱管理大量任务、决策与可复用工作流 | 阶段 1 的工件与结果 |
| 3. 隔离工作区编排 | 规划中 | 通过明确的原地、branch 或 worktree 策略运行相互独立的任务 | 阶段 2 权限模型与可恢复 effect 协议 |
| 4. 项目级任务与多 Agent 编排 | 规划中 | 拆解项目级交付项并协调并行 Codex 执行者 | 阶段 2 和 3 |
| 5. 扩展多仓库交付与持续保障 | 规划中 | 为已交付精确集合核心增加依赖感知复用和外部质量系统 | 阶段 1–4 |
| 6. 团队交付网络 | 规划中 | 移交、分派、批准、审计并可选择共享任务状态 | 稳定的任务、工件、身份与权限协议 |
| 7. 开放生态与自适应编排 | 规划中 | 发布可信交付能力并获得可解释建议 | 成熟的工作流、证据与团队运营模型 |

## 阶段 1 — 完整的个人交付（已在 0.3.0 交付）

### 已交付成果

开发者可以把功能、缺陷修复、调查、重构或快速变更，从初始契约推进到可审查、
经验证的终态结果。编排器在会话、保障发现、有界修复循环和已接受范围修订之间
保存完整交付过程。

### 已交付能力

- 结构化启动与最小启动都会生成带稳定验收 ID 的有界交付契约。
- 重复 `--repo` 将 1–8 个精确规范、用户预备的 Git 工作树根绑定为不可变任务成员；
  Hook 可以从任一无歧义成员恢复任务。
- 六个官方工作流提供快速、规划驱动、调查、重构与高保障个人交付路径。
- 类型化工件保留生产者、契约、源码快照、按仓库范围化的治理资源、输入血缘、摘要值
  和新鲜度。
- OpenSpec、codebase-memory 与独立审查具有已声明的可用、降级和不可用行为，
  控制器保持与工具无关。
- 自适应保障持久化所有执行、消耗绝对的义务/类别/返工预算，并将耗尽工作路由到未完成 Dossier。
- `dev-flow-workflow/0.3.0` 定义在严格多数的正常非终态阶段声明取消；交付收尾阶段始终不可取消。
- 契约修订创建新契约 revision source 并重新进入已声明规划；决策与精确豁免保持
  可归责、可重放。
- 保障计划精确覆盖验收标准及派生出的成员、集成、文档、手工证据和审查义务；后续改动
  只会使闭包相交的证据失效，无法证明不相交时则保守失效。
- 成功与未完成 Delivery Dossier 汇总当前覆盖、保障、文档、风险、决策、来源和移交；
  Delivery Dossier 0.3.0 还包含规范成员诊断。

### 交付证据

- 聚焦 0.3.0 用户旅程覆盖每个官方工作流、成功与耗尽、可选 driver 可用/降级路径、
  取消、决策以及契约修订恢复。
- 重启和过期 binding 路径保留唯一当前动作，并拒绝绑定过时输入或聚合快照的
  证据。
- 成员缺失或移动时，仓库相关进度不会写入部分证据；恢复精确持久化根之前仍可检查
  已存 ledger。
- 验收覆盖区分已证明、显式豁免和未验证标准；自审不会成为独立批准。
- 只要已安装快照身份与真实 Hook/Skill 接取需要宿主观察，它们就继续作为明确的
  发布证据字段。

## 阶段 2 — 交互式工作流工作台（首个只读切片已交付）

0.3.0 本地只读 Web UI 只交付本阶段的观察切片。更完整的工作台仍处于规划中，Web UI
不会因此获得状态变更或审批权威。

### 能实现什么

开发者可以管理多个长周期任务，看清每个任务为何等待，做出持久决策，并将
项目专属的交付方法沉淀为持久的产品配置。

### 核心能力

- 先通过稳定的 CLI/JSON 视图提供本地任务驾驶舱，随后扩展为 TUI 或本地 UI；
  展示搜索、筛选、优先级、标签、仓库、负责人、健康度、阻塞项、更新时间与下一动作。
- 提供任务时间线、工件与证据浏览器、恢复简报、决策卡片、批准收件箱以及下一步
  原因说明。
- 增加项目配置与任务模板，用于定义默认工作流、风险等级、保障策略、工具能力、
  仓库范围与保留策略。
- 支持命名动作结果，例如 approved（已批准）、changes-requested（请求修改）、
  failed（失败）、waived（已豁免）和 blocked（已阻塞）。
- 增加条件路由、可选阶段、有界重试与返工循环，以及暂停、恢复、阻塞、重试、豁免、
  批准、拒绝与操作员干预。
- 将每次批准或豁免绑定到精确的任务、动作实例、修订、工件摘要值、操作主体、角色与范围。
- 在创建任务前提供 `workflow validate`、`workflow explain` 与
  `workflow simulate`，涵盖正常路径、替代结果、必需能力、权限、工件与退出条件。
- 增加可复用阶段模板、类型化输入输出、子流程、保障配置，以及可视化工作流与证据图。
- 支持任务克隆、派生、归档、导出与显式保留，并完整保存活动证据。

### 里程碑验收证据

- 用户通过明确的任务身份与选择机制操作多个活动与终态任务。
- `review → rework → re-review` 始终可重放，并展示每一次尝试。
- 针对旧计划、旧 diff 或旧动作实例的批准，一律以失败关闭。
- 返工预算耗尽、批准人不可用、豁免、放弃与操作员接管，都有确定性结果。
- 至少三个官方工作流族复用相同的阶段与工件契约。
- CLI、Hook、Skill、TUI 与任何本地 UI 对同一修订展示相同 projection，并通过
  同一个控制器提交命令。

## 阶段 3 — 隔离工作区编排（规划中）

### 能实现什么

多个任务可以采用不同工作区策略，在同一个仓库中安全工作；与此同时，产品能够说明
所有权、冲突、恢复方式以及每一个获授权的 Git effect。

### 核心能力

- 允许任务独立选择原地工作、现有 branch、现有 worktree、明确授权的托管 branch，
  或明确授权的隔离 worktree。
- 记录工作区身份、所有者、租约、仓库、base ref、固定基线、预期变更范围、创建回执
  与当前健康状态。
- 在工作开始前检测 branch 和 worktree 命名冲突、路径重叠、基线漂移、用户未清理工作
  与相冲突的任务 claim。
- 将工作区规划与执行分离，让用户可以预览确切变更和所需权限。
- 引入可恢复的 effect 生命周期：
  `intent → authorize → execute → receipt → reconcile`。
- 将每项 Git 权限绑定到具体操作和目标。merge、push、删除与清理各自需要独立授权。
- 提供集成预览、变更集比较、冲突报告、放弃，以及对任务所拥有资源的精确范围清理。
- 将仅附着和完全只读作为一等操作方式。
- stash、reset、clean、commit、push、merge、force-push 与删除均需显式授权；
  删除与清理范围限定为任务拥有的资源。

### 里程碑验收证据

- 每个官方工作流都在其声明支持的仓库拓扑与工作区策略组合下通过已安装用户旅程。
- 资源创建后崩溃、租约过期、命名冲突、基线移动、用户编辑、脏 worktree 与部分 Git
  effect，都具备确定性的诊断与恢复路径。
- 所有权回执将每个任务的覆盖、接管与清理范围限定在自身资源内。
- 使用独立 worktree 的并发任务保持任务状态与工作区租约相互隔离。
- 权限回执将每次授权绑定到一个 Git effect 操作与目标。

## 阶段 4 — 项目级任务与多 Agent 编排（规划中）

### 能实现什么

一项大型单项目需求可以拆解为相互依赖的工作包，分派给并行 Codex 执行者，各自独立
恢复，并最终汇聚为一个证据完备的成果。

### 核心能力

- 引入项目级交付项、父任务与子任务、里程碑、显式依赖、批次、关键路径与聚合完成规则。
- 允许 Codex 根据交付契约与影响报告提出任务图；人工确认负责授权每次范围扩展与
  工作项创建。
- 在项目层暴露一个有界的可运行动作集合，每个已认领的执行者接收一个动作。
- 增加认领、租约、心跳、幂等完成 key、重新分派、重试、取消传播与操作员接管。
- 支持顺序工作、扇出、扇入、部分重试、可选子任务、集成屏障与故障隔离。
- 为每个执行者提供最小上下文包，其中包含已接受范围、基线、依赖、工件、工作区、
  预期输出与 join contract。
- 强制执行并发预算、工作区与仓库租约、组件所有权以及可配置的资源限制。
- 提供项目级交付项组合视图，展示可运行工作、执行者泳道、阻塞依赖、
  关键路径、证据缺口与集成就绪度。
- 保持作者与独立审查者角色可分离、可归责。

### 里程碑验收证据

- 每个动作实例同一时刻最多由一个执行者拥有。
- 执行者故障与租约过期可以恢复，并保持完成结果恰好计数一次与已接受工件持久化。
- 项目组合视图在子任务失败后继续展示已完成的同级任务，重试调度待完成工作。
- 父任务完成必须具备精确的必选子任务集合与 join 证据。
- 已安装用户旅程覆盖顺序拆解、扇出/扇入、部分失败、重新分派、取消与操作员接管。
- 并行执行能够证明交付前置时间缩短，同时保持工作区隔离，并使未解决冲突率稳定
  或下降。

## 阶段 5 — 扩展多仓库交付与持续保障（规划中）

### 能实现什么

在已交付的本地精确集合核心之上，一个交付计划可以增加仓库角色与依赖顺序，安全保留
可独立复用的证明，并向 Issue、PR、CI 与发布系统回传可信证据。

### 核心能力

- 为现有精确仓库集合增加角色、组件范围、所有权、依赖与集成顺序。
- 既支持 monorepo 组件范围，也支持真正的多仓库变更集。
- 将每个仓库的工作区策略与工作流深度、其他仓库的策略分别建模并独立选择。
- 执行跨仓库影响分析、依赖感知规划、可并行实施、逐仓库验证、集成验证与统一收尾。
- 将依赖感知复用扩展到导入的 CI 与发布证据，同时保留当前控制器的切片相交与集成闭包
  失效规则。
- 增加保障图，将每项验收标准映射到计划、代码变更、聚焦测试、审查发现、CI checks、
  集成结果、文档与发布工件。
- 跟踪证据来源与新鲜度。代码、依赖、基线或配置变更会使依赖这些输入的证据
  精确失效。
- 为 OpenSpec、codebase-memory、Issue 跟踪器、Git 托管平台、CI 系统与工件存储提供
  显式连接器。
- 导入外部上下文与证据；若连接器可以改变外部状态，则要求范围受控的授权与
  持久化回执。
- 生成仓库级和聚合的 PR 就绪或发布就绪 Delivery Dossier，其中包含依赖顺序、
  契约符合性结果、未解决项与移交说明。

### 里程碑验收证据

- 真实的 API/consumer、library/application 与协同发布用户旅程覆盖链式、
  扇出/扇入和部分失败拓扑。
- 角色与依赖准入扩展已交付的精确集合预检，同时不削弱规范成员关系和缺失成员恢复。
- 恢复过程保留可独立接受的逐仓库证明，并精确调度因依赖而失效的单元。
- 完成要求具备所有必选的逐仓库证据与全局集成证据。
- 代码或依赖变更会在继续执行前使关联证明失效。
- 外部超时、权限拒绝、重复回执、部分成功与离线恢复，都会产生安全、可恢复的状态。
- 验证矩阵明确将工作流深度、仓库拓扑、工作区策略与执行拓扑作为相互独立的维度覆盖。

## 阶段 6 — 团队交付网络（规划中）

### 能实现什么

开发工作可以在人、机器与角色之间流转，并完整保留已接受范围、决策、工件、
工作流身份、证据与当前动作。

### 核心能力

- 从本地、已脱敏、经过完整性校验的移交 bundle 起步，使另一套受信任安装能够检查
  并导入。
- 增加请求者、实施者、审查者、批准者、操作员与工作流所有者角色。
- 支持分派、认领、审查与批准队列、评论、提及、通知、决策期限与显式升级。
- 发布项目和组织策略包，用于工作流选择、保障级别、必需审查者、权限、
  保留策略与允许使用的连接器。
- 将动作、批准、豁免、干预与外部 effects 归因到经过身份验证的操作主体与精确修订。
- 提供团队视图，展示等待实施、审查、批准、集成、移交或恢复的工作。
- 当身份、授权、并发、隐私、离线运行、数据所有权、可用性、支持与删除模型均形成
  完整的已发布契约时，增加可选择启用的共享任务 hub。
- 共享运行模式提供身份验证、RBAC、加密、项目与组织边界、事件排序、离线冲突处理、
  审计导出与保留控制。
- 所有界面，包括集成与团队 UI，都遵循同一个控制器权威与回执模型。

### 里程碑验收证据

- 导出与导入能够精确保留需求、投影、工作流身份、工件摘要值、决策与待处理动作。
- 脱敏标记每个被移除的必需字段，并据此设置 bundle 完整性状态。
- 授权策略将受保护工件读取、工作认领、批准、豁免与连接器调用限定给获准主体。
- 并发批准、重复请求、租约过期、离线变更与服务恢复，都能保留确定性的任务历史。
- 团队能够审计一次交付中由谁决策、实施、审查、批准、豁免和干预。
- 共享协调始终可选；个人本地工作流保留其全部受支持能力。

## 阶段 7 — 开放生态与可解释的自适应编排（规划中）

### 能实现什么

团队可以把可信开发方法作为可复用产品发布；面对复杂工作，编排器可以在明确接受的
范围、保障级别与权限内推荐可解释的交付策略。

### 核心能力

- 提供面向工作流、工件、保障、策略与连接器包的创作工作台，
  包含可视化组合、验证、模拟、一致性 fixtures 与契约/能力预览。
- 发布带版本的工作流包、能力清单、项目配置、策略包与连接器契约。
- 增加可信注册中心，提供签名、来源、权限预览、版本/能力协商、固定版本、撤销、
  健康状态与可复现安装。
- 为纯数据扩展、只读驱动和可以产生外部 effects 的连接器定义独立能力类别。
- 第三方可执行连接器通过隔离的连接器协议运行在核心控制器之外，并采用范围受控的
  请求与可归责响应。
- 将连接器访问范围限定在声明的仓库、网络目标、密钥、工件类型、操作与保留规则。
- 根据需求、影响面与本地项目策略，推荐工作流、保障配置、任务图、仓库范围、
  工作区策略、并发预算、审查者与集成门禁。
- 识别潜在阻塞、缺失证据、过期决策、返工风险、集成风险与关键路径变更。
- 从本地、主动选择加入且已接受的决策与成功模板中学习可复用项目惯例；本地选择
  加入的证据构成学习来源。
- 让每条建议都可解释、可编辑、可拒绝、带版本且可重放。
- 新增原生平台支持以前，launchers、locking、path safety、Hook 行为、安装、effects
  与端到端用户旅程均需具备原生证据和维护者。

### 里程碑验收证据

- 契约、能力与最小权限检查为每次扩展安装和调用提供门禁。
- 撤销在本地获知或签名有效期到期时生效；连接器暂时中断期间，任务进度保持持久化。
- 第三方连接器的访问范围限定为其声明的资源，并为每个 effect 留下可归责的回执。
- 工作流作者通过软件包 schema 与工具完成构建、模拟、发布、升级、诊断与撤销。
- 每次由建议驱动的权限授予、工作创建、保障变更或范围变更，都以明确接受为前提。
- 受控内部试用同时度量返工减少、交付前置时间缩短与自动化规模。

## 跨领域交付基础

每个功能阶段都通过以下赋能轨道推进。各阶段推进并验证所有适用轨道，并为完整的
轨道集合记录适用性证据。

| 基础轨道 | 必需产品能力 | 发布证据 |
|---|---|---|
| 可复现发布 | 验证精确的已跟踪候选版本，识别每个已安装快照，并复现软件包、Skill、清单与文档证据 | 每个发布版本都能从已跟踪文件树复现软件包、Skill、清单、工作流身份与双语文档验证 |
| 身份与版本权威 | 分离发布、状态格式、工作流语言、已选工作流、能力、连接器、工件与 Agent 协议身份；版本变更前对活动任务分类 | 版本变更模拟对每个活动任务分类，并验证每项决策对应的身份归属域 |
| 诊断与数据生命周期 | 提供 `version`、`doctor`、完整健康清单、稳定错误目录、有界支持导出、保留、归档、导入与精确范围移除 | 只读健康视图完整列出任务条目、已安装快照、能力状态以及可用的恢复或移除操作 |
| 安全与可恢复 effects | 限定输入与锁；为外部 effects 持久化意图、授权、回执、重试与核对 | 已安装 effect 用户旅程证明范围授权、幂等回执处理、中断恢复与精确资源所有权 |
| 安全与隐私 | 默认本地存储、最小权限、脱敏、显式数据出站、密钥隔离、操作主体归因与撤销 | 移交与连接器用户旅程证明脱敏、显式出站、密钥隔离、操作主体归因与撤销 |
| 已安装质量证据 | 将变更面映射到最小聚焦测试，验证已安装用户旅程，保持审查证据与快照绑定，并发布每个声明组合的验证证据与支持状态 | 聚焦契约与已安装端到端用户旅程覆盖每个声明组合，并将审查证据绑定到精确快照 |
| 平台与文档证据 | 发布具备原生证据的平台与解释器声明；保持安装、恢复、移除以及中英文产品文档一致 | 每项平台声明都具备原生安装、Hook、锁、路径安全、恢复、移除与文档一致性证据 |

## 产品护栏

雄心勃勃的能力扩张必须保留让委派值得信任的关键属性：

1. **控制器是唯一的状态转换写入者。** CLI、Hook、Skills、UI、Agents 与
   连接器都向同一个权威提交命令。终止图节点成员关系定义完成状态；Hook
   提供建议，并在内部错误时放行。
2. **一个已认领的执行者对应一个清晰的当前动作。** 当前 0.3.0 即使覆盖多个仓库，
   仍只有一个 Codex 和一个任务级动作。未来项目图可以暴露一个有界的可运行集合，
   每个动作的所有权保持明确。
3. **工件与证据必须携带血缘。** 证明需要标明其输入、基线、生产者、时间、schema 与
   摘要值；输入发生变化时，受影响的证明随之失效。
4. **人工权限必须明确且狭窄。** 批准、豁免与 effect 权限绑定到精确的任务、动作、
   修订、操作主体、范围与目标。
5. **effect 必须经过规划、授权、回执记录，并且可以恢复。** 宿主权限提示用于请求
   宿主执行访问；控制器记录的工作流批准提供工作流权限。
6. **用户工作通过明确且范围受控的授权变更。** stash、reset、clean、commit、push、
   force-push 与清理各自需要授权；清理范围限定为任务拥有的资源。
7. **工作流深度、任务拓扑、仓库拓扑、工作区策略与协作模式始终是相互独立的产品维度。**
8. **任务状态始终位于目标仓库之外。** 默认采用本地优先运行并保护私有数据；共享与
   网络访问必须主动选择启用，且范围受控。
9. **扩展必须声明式、带版本且受权限约束。** 核心运行时完全使用 Python 标准库
   实现，并通过声明式契约接入扩展能力。数据包采用纯数据权限配置。仓库、密钥与
   变更访问分别需要显式能力声明与授权。可执行和联网连接器逻辑运行在核心控制器
   之外，并受强制能力边界与隔离连接器协议约束。
10. **数据演进以实际发布内容为准。** 数据变换、恢复与弃用服务于真实的持久化数据和明确的
    发布版本。

## 产品度量

度量来自已安装验收证据与主动选择加入的本地报告。

| 度量 | 期望结果 |
|---|---|
| 证据完整交付率 | 完成时，每项已接受标准都有新鲜证明或显式豁免 |
| 首个安全动作耗时 | 需求以最少设置进入风险适当且获授权的下一动作 |
| 恢复可靠性 | 中断的个人、子任务、工作区与多仓库任务在同一有效意图上恢复 |
| 返工收敛度 | 验证失败与审查发现通过有界修复在持久任务内闭环 |
| 人工决策延迟 | 批准与例外队列让等待可见，并缩短决策等待周期 |
| 并行交付有效性 | 多 Agent 工作缩短前置时间，同时保持唯一所有权、工作区隔离以及未解决集成风险稳定或下降 |
| 多仓库集成成功率 | 部分工作能够安全恢复，精确变更集最终通过集成验证 |
| 移交保真度 | 导入的工作完整保留范围、决策、工件、证据与待处理动作 |
| 工作流复用度 | 官方与项目工作流包取代反复拼装提示词，同时保持适配度与保障水平 |
| 过期证明防范 | 输入变化会在完成或交付前使受影响证据失效 |

## 路线图治理

- 对当前阶段与下一个阶段保持细化；后续阶段的进入证据就绪时，将产品成果与
  能力契约展开为详细规划。
- 通过一项 OpenSpec change 推动功能晋级，其中定义 personas、已安装用户旅程、
  独立产品维度、权限、数据与 effect 生命周期、失败与恢复、隐私以及可度量证明。
- 使用真实内部试用与外部用户证据确定阶段内部优先级。现有证据决定实施排序，
  北极星产品方向保持稳定。
- 将跨领域基础作为每项受影响功能的发布门禁持续维护。
- 保持 `ROADMAP.md` 与 `ROADMAP_CN.md` 语义一致。
- 让路线图聚焦已发布能力与产品承诺，并将已完成实施细节记录在发布档案中。

## 路线图能力的完成定义

一项能力在满足以下全部条件时完成：

1. 其承诺的用户成果能在已安装插件中实现；
2. 能力矩阵中每个受支持组合都有显式验收证据；
3. 与该能力相关的中断、并发、权限拒绝、部分 effects、重试、恢复、取消与操作员干预
   均得到处理；
4. 权限、状态归属、工件血缘、工作区所有权、effects、隐私与依赖方向均清晰明确；
5. 最小范围的聚焦契约测试与已安装端到端用户旅程全部通过；
6. 诊断、版本演进、恢复限制、保留、移除、打包与双语文档已按受影响范围更新；
7. 独立只读审查确认实施、证据与路线图成果三者一致。
