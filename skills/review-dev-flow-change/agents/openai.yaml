interface:
  display_name: "Review Dev Flow Change"
  short_description: "Independently gate the complete Git change snapshot"
  default_prompt: "Use $review-dev-flow-change to independently review this V5 change before handoff."
dependencies:
  tools:
    - type: "mcp"
      value: "codebase-memory-mcp"
      description: "Uses the explicitly selected current workspace project for review discovery"
policy:
  allow_implicit_invocation: false
