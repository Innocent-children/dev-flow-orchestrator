interface:
  display_name: "执行开发流程"
  short_description: "在一至八个精确仓库工作树中执行 0.3.0 结构化个人交付工作流"
  default_prompt: "使用 $follow-dev-flow 在一个或多个用户预备 Git 工作树中启动或继续 0.3.0 交付任务，并由一个 Codex 执行唯一的当前动作。"
dependencies:
  tools:
    - type: "mcp"
      value: "codebase-memory-mcp"
      description: "Provides per-member phase-selected impact discovery with distinct baseline/current project IDs"
policy:
  allow_implicit_invocation: true
