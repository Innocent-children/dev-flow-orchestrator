interface:
  display_name: "执行开发流程"
  short_description: "运行可恢复、逐状态确认的受控开发流程"
  default_prompt: "使用 $follow-dev-flow 启动或安全恢复这项开发请求，并在每次状态切换前用中文征求确认。"
dependencies:
  tools:
    - type: "mcp"
      value: "codebase-memory-mcp"
      description: "Maintains explicit baseline and current-workspace projects and traces change impact"
