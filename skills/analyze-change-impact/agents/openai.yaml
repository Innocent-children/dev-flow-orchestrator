interface:
  display_name: "Analyze Change Impact"
  short_description: "Map V6 impact with confirmed graph evidence"
  default_prompt: "Use $analyze-change-impact to produce a bounded, phase-selected, source-confirmed V6 impact report."
dependencies:
  tools:
    - type: "mcp"
      value: "codebase-memory-mcp"
      description: "Uses distinct baseline/current project IDs for phase-selected discovery"
policy:
  allow_implicit_invocation: false
