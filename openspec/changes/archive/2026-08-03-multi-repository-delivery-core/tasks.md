## 1. Current Product Authority

- [x] 1.1 Define the authoritative one-to-eight exact repository-set model and include its topology and accepted schema vocabulary in `PRODUCT_IDENTITY`.
- [x] 1.2 Accept only the current workflow schema; bind selected-workflow identity to selector, schema, and canonical document; remove workflow-adapter identity from task and workflow models.
- [x] 1.3 Validate immutable canonical repository tuples, unique IDs and roots, derived set identity, and one-member sets through the same path as every larger set.
- [x] 1.4 Make the controller Python API accept only `repositories`; remove the singular `repository=` alias while retaining one CLI `--repo` as a one-member set.

## 2. Snapshot, Runtime, and Recovery

- [x] 2.1 Emit and validate the current repository-set snapshot for every repository-backed operation, with one current member snapshot per canonical repository and no cardinality-selected top-level dialect.
- [x] 2.2 Require explicit repository IDs for every repository-backed resource and preserve scoped equality for matching relative paths in different members.
- [x] 2.3 Make planning, apply, contract revision, replay, freshness, cancellation, task views, and discovery consume the complete repository set through one record and action-binding model.
- [x] 2.4 Emit only the current agent projection with one `repository_set`, one current action, and member-specific blocked diagnostics.
- [x] 2.5 Preserve bounded revision compare-and-swap, retry, restart, recovery, cancellation atomicity, and stale-evidence behavior within the current model.

## 3. Verification and Delivery Dossier

- [x] 3.1 Validate the structured `criteria`, `repositories`, and `integration` coverage contract for every repository-set cardinality.
- [x] 3.2 Require exact repository coverage, top-level/integration command equality, command-aggregate consistency, and proven-or-currently-waived criterion coverage.
- [x] 3.3 Generate only the current Delivery Dossier with canonical member snapshots, scoped resources, all verification and review attempts, aggregate freshness, outcome, and handoff.

## 4. Remove Alternate Product Paths

- [x] 4.1 Remove alternate workflow loading, adapter identity, singleton snapshot/projection/coverage/Dossier branches, and cardinality-based schema dispatch.
- [x] 4.2 Remove retained-generation data handling, rollback guidance, migration, legacy detection, schema fallback, and compatibility replay paths.
- [x] 4.3 Remove the unused predecessor `GitClient.inspect()` projection while retaining the current bounded `snapshot()` boundary.
- [x] 4.4 Remove or rewrite tests that assert an alternate schema generation while retaining current-task revision, retry, recovery, replay integrity, and stale-evidence coverage.

## 5. Skills, Package, and Product Guidance

- [x] 5.1 Update the three packaged Skills and agent metadata for the exact repository-set model, explicit resource scope, one Codex/current action, and user-owned Git effects.
- [x] 5.2 Update Hook guidance, manifest metadata, package validation, installed journeys, architecture, installation, contribution, roadmap, and bilingual README guidance to express only the current model.
- [x] 5.3 Validate every Skill with the bundled skill-creator validator and validate the plugin manifest/package.

## 6. Focused Verification and Review

- [x] 6.1 Add focused one-member and multi-member tests proving the same snapshot, projection, coverage, resource, and Dossier contracts.
- [x] 6.2 Run the focused relevant unittest modules on macOS and record commands and results without running prohibited full-suite or native cross-platform validation.
- [x] 6.3 Strictly validate this OpenSpec change and run both a one-member installed journey and the selected larger-set installed journeys.
- [x] 6.4 Complete an independent implementation review, address confirmed in-scope findings, and rerun affected focused checks.

## 7. Unified Pre-Release Version

- [x] 7.1 Define the sole authoritative `PRODUCT_VERSION = "0.2.0"` and derive every runtime schema, digest domain, namespace, task/workflow version, projection, Dossier, receipt, and evidence identifier from it.
- [x] 7.2 Set plugin manifest, Python package metadata, lock data, and every official workflow document to exact version `0.2.0`; make package validation reject any current-product version drift.
- [x] 7.3 Remove generation-coded V5/V6 and component-coded v1/v2/v3 names from current runtime symbols, tests, CI, Skills, Hook guidance, installed journeys, and public documentation without adding compatibility or migration code.
- [x] 7.4 Add focused version-authority, embedded-schema, candidate-scan, and mismatch coverage, then run affected macOS tests, strict OpenSpec validation, package/manifest validation, every bundled Skill validator, and installed journeys.
- [x] 7.5 Complete an independent review of the unified-version increment and rerun any checks affected by confirmed findings.
