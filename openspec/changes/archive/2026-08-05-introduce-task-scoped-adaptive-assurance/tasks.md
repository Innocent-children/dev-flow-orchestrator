## 1. Establish the clean 0.3.0 protocol boundary

- [x] 1.1 Advance the product, workflow, schema, digest-domain, projection, record, artifact, and package identities in `product.py`, `model.py`, and related authorities to the internally consistent `0.3.0` family.
- [x] 1.2 Introduce the separate 0.3 controller data namespace in `store.py` and task discovery, and make the 0.3 runtime ignore retained 0.2 namespace bytes without reading, rewriting, migrating, repairing, or deleting them.
- [x] 1.3 Replace persisted-state, action-input, artifact, snapshot, binding, coverage, review, projection, and Dossier validators with strict 0.3 shapes; explicitly supplied non-0.3 values must fail as unsupported without conversion or fallback.
- [x] 1.4 Encode the normative product maximums as shared runtime authorities and package assertions: 4,096 snapshot paths per repository, 12,288 index stage entries per repository, 2 MiB index command output, 128 ownership claims per source action, 4,096 current roll-forward manifest entries, 128 impact entries, 64 obligations, 64 findings, 64 evidence items per execution, and 256 workflow actions per effective contract, all additionally subject to the existing action-payload and text-field byte limits.
- [x] 1.5 Add protocol-boundary and replay tests proving valid 0.3 state survives restart, mixed-version records fail closed, and an old 0.2 data directory is neither discovered nor modified by 0.3 operation.

## 2. Make Git evidence index-exact

- [x] 2.1 Extend `git_client.py` and `snapshot.py` to capture the complete canonical stage 0-3 entry set for every relevant path, including modes and validated object IDs alongside each worktree observation and gitlink state.
- [x] 2.2 Include the ordered index-entry set in repository and repository-set snapshot digests, action bindings, stability checks, and serialization while retaining bounded two-pass all-or-none capture and enforcing the 4,096-path, 12,288-index-entry, and 2 MiB index-output limits.
- [x] 2.3 Reject source successors, assurance evidence, and successful finalization when unmerged index stages are present, while preserving their read-only diagnostic representation.
- [x] 2.4 Expand `tests/test_git_snapshot.py` for staged-blob replacement with unchanged worktree/status, symlink and gitlink index identity, alternate Git object formats, index races, unmerged entries, exact-at-limit and plus-one path/index-count/output ceilings, and atomic no-truncation rejection.
- [x] 2.5 Extend multi-repository snapshot tests to prove one member's index instability rejects the aggregate capture without partial state or evidence mutation.

## 3. Implement task-owned change capsules and leases

- [x] 3.1 Add canonical 0.3 preflight-baseline and task-change-manifest models with per-member HEAD, worktree, relevant-path complete index entry sets, resource, predecessor, producer, criterion, classification, purpose, and digest provenance.
- [x] 3.2 Derive cumulative per-path manifest successors from the immutable preflight ownership origin and each bound source interval, and require agent claims to cover exactly the controller-observed changed paths with no omissions, duplicates, cross-root paths, incompatible criterion mappings, or overflow.
- [x] 3.3 Classify preflight environment state, accepted task-owned changes, and later ambient drift separately; block repository-dependent progress on unclaimed drift with exact repository/path diagnostics and the restore, contract-revision, or authorized-cancellation choices.
- [x] 3.4 Apply manifest successors atomically across the exact repository set and preserve complete lineage through source production, documentation, rework, contract revision, replay, and terminal Dossier generation. Treat each revision source only as the next interval anchor and roll forward every still-material inherited entry, exact adopted-drift entry, and later entry without replacing the immutable preflight origin.
- [x] 3.5 Add a data-directory-wide membership admission lock and derive one active lease per canonical member root plus worktree-specific Git administrative directory from non-terminal current-namespace task state; reject the complete start with owning-task diagnostics on either exact overlap, but permit distinct linked worktrees that merely share a Git common directory.
- [x] 3.6 Extend current-namespace task discovery to match every member repository, make any unresolved corrupt 0.3 inventory entry block all new admission without implying lease release, keep read-only corruption diagnostics available, and never inspect retained 0.2 namespaces during 0.3 discovery.
- [x] 3.7 Add focused capsule tests for clean, pre-dirty, staged-only, unstaged, untracked, renamed, deleted, symlink, gitlink, conflicting-claim, ambient-drift, retry, tamper, restart, ordinary contract revision after implementation, and drift adoption, proving the current manifest carries every still-material owned entry and exact provenance across revisions.
- [x] 3.8 Add lease and multi-repository tests for canonical aliases, exact worktree-specific Git-directory aliases, linked worktrees sharing a common directory across distinct tasks, same-task duplicate-common-directory rejection, simultaneous starts, partial-overlap rejection, terminal reuse, corruption-before-terminal admission failure, and all-or-none mutation.

## 4. Derive and dispatch adaptive assurance plans

- [x] 4.1 Add canonical impact-manifest, assurance-plan, obligation, prerequisite, evidence-contract, invalidation-rule, allowance, aggregate-ceiling, and plan-digest models in the pure domain layer.
- [x] 4.2 Implement and validate the closed `dev-flow-assurance-policy/0.3.0`: the exact six-profile floor matrix and per-obligation caps, `source-confirmed` as the only focused confidence, conservative handling for `degraded`, `partial`, and `unknown`, the eight closed risk triggers, canonical grouping, complete criterion coverage, acyclic prerequisites, fixed numeric bounds, and deterministic order; reject custom workflows that weaken any rule.
- [x] 4.3 Derive repository, integration, documentation, manual-evidence, and independent-review obligations plus the `bugfix` regression evidence contract from the accepted contract, current roll-forward manifest, impact evidence, and the closed policy. Group at most one repository check per member, one integration check per distinct evidence contract over sorted required edges, and one documentation, manual-evidence, and review obligation; overflowed impact becomes reasoned `unknown` and the conservative obligation set rather than a truncated focused plan.
- [x] 4.4 Implement deterministic `assurance.dispatch` so `next` projects exactly one currently eligible outstanding obligation and bypasses verification or review dimensions that the controller has derived as `not-required`.
- [x] 4.5 Persist every assurance or rework execution against its obligation and effective contract and charge pass/fail/unavailable/superseded attempts exactly once against per-obligation, class, and total-action ceilings. Charge each separately persisted reuse, waiver, finding disposition, or prerequisite refresh exactly one total-action unit and no assurance/rework unit; charge neither controller-derived `not-required` state nor a read-only reuse derivation.
- [x] 4.6 Compute finite `maximum_remaining_actions` for every current projection and reject workflow or plan graphs whose remaining route is unbounded or cannot fit within the declared ceilings.
- [x] 4.7 Implement slice-aware invalidation and reuse by comparing manifest deltas, governing inputs, criterion meaning, member/integration closure, and impact confidence; invalidate conservatively when any dependency is stale or ambiguous.
- [x] 4.8 Add at least one focused and one triggered assurance-domain test for each of `lite`, `feature`, `bugfix`, `investigation`, `refactor`, and `full`, including public-documentation criteria, regression evidence, investigation without fabricated implementation, invariant coverage, closed risk triggers, unknown-impact fallback, disjoint reuse, and intersection-only invalidation.
- [x] 4.9 Add budget and dispatch tests for every normative profile formula and per-obligation cap; derive canonical `U` as the sum of `max(allowance - 1, 0)` over source-rework-capable obligations in the initial conservative budget-reservation set and assert exact `rework_ceiling = min(profile_rework_cap, U)` for zero, below-cap, at-cap, and above-cap cases. Cover grouped finding-bound rework, execution-time retry consumption, the exact 256-action total formula, persisted reuse/governance/prerequisite/finalizer charging, deterministic order, failures, unavailable drivers, restart, conflict replay without double charging, same-contract replacement with identical reservation set and ceilings, contract revision, exhaustion, and exact monotonic `maximum_remaining_actions`.

## 5. Enforce causal review governance

- [x] 5.1 Add canonical independent-review and review-finding models with stable IDs and fingerprints, severity, blocking flag, causal relation, criterion/member/location evidence, smallest sufficient resolution, reviewed slice, plan, guidance, reviewer, and snapshot bindings.
- [x] 5.2 Validate `introduced`, `affected`, `pre-existing`, `out-of-scope`, and `unknown` classifications. Require `affected` to prove a bounded causal path from a current roll-forward manifest entry; classify proof outside the current impact closure as an impact gap that invalidates impact evidence and the plan instead of rejecting or hiding the finding.
- [x] 5.3 Derive `approved`, `changes-requested`, `triage-required`, or `unavailable` in the controller. Create rework obligations only for current blocking `introduced` or in-closure `affected` findings; a blocking `unknown` keeps review unresolved without source rework until bounded causal refresh or an authorized disposition, while non-blocking unknown, pre-existing, and out-of-scope observations remain visible without task rework.
- [x] 5.4 Implement finding-bound `accepted-risk`, `confirmed-out-of-scope`, and `expand-contract` dispositions with actor, rationale, contract digest, revision CAS, authority checks, and immutable historical lineage. Permit exact accepted risk to resolve blocking causal or unresolved-causality status; require contract revision only when scope or criteria change, not merely to replan a proven impact gap.
- [x] 5.5 Record rework, closure, retention, replacement, stale-review, and re-review links without mutating historical finding content or accepting an agent-authoritative aggregate verdict.
- [x] 5.6 Add review tests proving unrelated and pre-existing issues never schedule rework, indirect effects require causal evidence, a blocking unknown cannot approve or rework until triage/disposition, an out-of-closure affected finding invalidates and replans under the same contract before rework, contradictory verdicts are rejected, dispositions do not fabricate evidence, and only atomic contract expansion can adopt new scope.

## 6. Integrate replay, coverage, projections, and finalization

- [x] 6.1 Extend engine transitions and persisted-state replay to validate immutable preflight origin, revision-anchor and roll-forward manifest lineage, lease, plan, obligation, execution, finding, triage, impact-gap, disposition, budget, and slice-freshness invariants before deriving current state.
- [x] 6.2 Update `Controller.start`, `next`, `apply`, `show`, cancellation, contract revision, and decision mutations to preserve one revision-CAS mutation boundary and one current action across lease admission and dynamic assurance dispatch.
- [x] 6.3 Update verification coverage to bind each criterion proof to its exact obligation, executed or reused evidence, manifest slice, impact closure, repository/integration target, waiver authority, and currentness; represent omitted dimensions only as controller-derived `not-required` states.
- [x] 6.4 Generate the 0.3 Delivery Dossier from current records with immutable preflight origin, revision anchors, roll-forward capsule inventory, ambient drift, plan rationale, obligation states, reuse/skip bases, exact budget use, structured finding lineage, coverage, risks, and an explainable `DONE` or `INCOMPLETE` decision; unresolved drift, triage, impact gap, required evidence, or budget authority must block `DONE`.
- [x] 6.5 Update compact agent projections, CLI output, and Hook context with the current obligation, task-change slice, `triage-required` or impact-gap state, budget counters, `maximum_remaining_actions`, finding summary, and Dossier summary while keeping full content in read-only task views.
- [x] 6.6 Add controller, stale-mutation, store-integrity, delivery-runtime, CLI, and Hook tests for atomic apply, tamper rejection, restart, stale bindings, revision carry-forward, drift adoption, ambient drift, unknown triage, impact-gap replanning, selective reuse, exact governance charging, exhausted finalization, cancellation, and compact resumability.

## 7. Convert workflow and agent contracts

- [x] 7.1 Extend custom-workflow parsing and validation with typed `assurance.dispatch`, obligation completion/rework/triage/impact-gap targets, the closed policy reference, finite-budget checks, closed handler vocabulary, and deterministic incomplete finalization; reject custom policy overrides that weaken official confidence, trigger, coverage, grouping, or budget constraints.
- [x] 7.2 Convert all six official workflow YAML files to 0.3 obligation dispatch, embed the exact `dev-flow-assurance-policy/0.3.0` profile selection and budget class in each file, and retain explicit preflight, impact, planning, source, contract-revision, cancellation, and Dossier stages.
- [x] 7.3 Replace fixed-loop assertions with portfolio tests proving each profile's focused and triggered obligation set, selective re-dispatch only for closure-intersecting evidence, workflow depth independent of repository count, and absence of an unconditional all-member verification or review loop except where the `full` or conservative policy explicitly requires it.
- [x] 7.4 Update `skills/follow-dev-flow` to follow only the projected obligation, submit exact ownership claims, record degraded optional-driver status, honor remaining recorded attempts, and stop at controller-derived terminal authority.
- [x] 7.5 Update `skills/analyze-change-impact` to produce bounded evidence-backed impact closures and confidence, and to request conservative assurance when scope cannot be established.
- [x] 7.6 Update `skills/review-dev-flow-change` to review the complete roll-forward task-owned slice and its direct or indirect effects, emit bounded structured causal findings, preserve blocking unknowns for triage, report impact gaps, and keep pre-existing/out-of-scope observations visible without requesting task rework.
- [x] 7.7 Synchronize packaged Skill metadata and Hook contracts with the 0.3 identities, then add validation tests that reject stale guidance, unsupported capabilities, and agent claims of controller authority.
- [x] 7.8 Advance plugin manifests, bootstrap/install entry points, packaged workflow indexes, and installer validation to the single 0.3 family; remove every 0.2 loader or compatibility route while leaving retained old namespace bytes outside discovery and mutation.

## 8. Prove candidate and installed behavior

- [x] 8.1 Update `scripts/validate_package.py` to enforce the complete 0.3 identity set, every exact numeric authority and profile-policy rule, workflow catalog, schema references, adaptive-assurance wording, Skill/Hook/plugin/installer metadata, and absence of legacy discovery, loading, replay, translation, repair, or migration entry points.
- [x] 8.2 Extend candidate and unit validation with one focused and one triggered journey for each official profile plus roll-forward contract revision, adopted drift, unknown-causality triage, out-of-closure impact-gap replanning, unrelated findings, causal rework, evidence reuse/invalidation, exact counter charging, budget exhaustion, linked-worktree concurrency, corrupt-current-inventory admission failure, and exact multi-repository targeting.
- [x] 8.3 Update `scripts/validate_installed_stage1.py` and installed-journey tests to exercise clean, pre-dirty, staged, unstaged, untracked, multi-repository, linked-worktree, restart, conflict, disposition, triage, impact-gap, contract-revision carry-forward, drift adoption, corrupt-inventory, cancellation, and incomplete-Dossier paths from an installed candidate.
- [x] 8.4 Add an installed clean-cut journey that places retained 0.2 bytes outside the 0.3 namespace and proves 0.3 neither discovers nor mutates them, while explicitly supplied 0.2 protocol values fail closed.
- [x] 8.5 Add exact-at-limit and plus-one validation for every product bound, including atomic no-truncation and reasoned unknown-impact fallback, then run the focused snapshot, capsule, lease, assurance, review, workflow, controller, delivery, Hook, CLI, package, and installed-journey suites and record every pass, failure, skip, and environment limitation without extrapolating partial results.

## 9. Synchronize public documentation

- [x] 9.1 Update the English `README.md`, `ROADMAP.md`, `ARCHITECTURE.md`, `CONTRIBUTING.md`, and `INSTALL.md` for task-owned capsules, user-prepared worktrees, adaptive obligations, causal review, selective evidence reuse, absolute recorded-attempt budgets, 0.3 clean-cut installation, and recovery boundaries.
- [x] 9.2 Translate and fully synchronize those changes into `README_CN.md`, `ROADMAP_CN.md`, `ARCHITECTURE_CN.md`, `CONTRIBUTING_CN.md`, and `INSTALL_CN.md`, preserving commands, paths, versions, links, scope, and language switch links.
- [x] 9.3 Regenerate or update any package/demo documentation assets affected by the 0.3 projection, and verify public claims against candidate and installed validation evidence.

## 10. Final verification

- [x] 10.1 Run the complete project test suite in the project `uv` environment and resolve only failures causally introduced by this change without weakening requirements or tests.
- [x] 10.2 Run strict candidate-package validation and all installed acceptance journeys from a fresh temporary installation, confirming the runtime remains Python standard-library-only.
- [x] 10.3 Run `openspec validate introduce-task-scoped-adaptive-assurance --type change --strict` and reconcile the delivered implementation, specifications, tasks, public docs, and recorded evidence before reporting completion.
