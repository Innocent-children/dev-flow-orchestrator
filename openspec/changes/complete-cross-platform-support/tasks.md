## 1. Portable Controller Foundations

- [x] 1.1 Add standard-library test helpers for child Python processes, isolated environments, Unicode/space paths, and capability-scoped assertions without POSIX utility dependencies.
- [x] 1.2 Implement verified POSIX and Windows exclusive-lock backends over a stable byte range, make acquisition/release errors structured and fail closed, and cover task/config/workspace lock scopes.
- [x] 1.3 Add native multiprocess contention, stale-revision, lock-failure, and process-death recovery tests proving that no mutation executes unlocked.
- [x] 1.4 Implement the exact 1–64 byte ASCII task-ID grammar, trailing-dot and Windows reserved-stem rejection, ASCII-lowercase identity, and a serialized namespace collision check while preserving readable portable schema-v1 tasks.
- [x] 1.5 Implement non-empty environment resolution for explicit/plugin/native data directories and actor selection, including Windows `LOCALAPPDATA`/`USERNAME`, macOS Application Support, Linux XDG, and home fallbacks.
- [x] 1.6 Implement filesystem-aware identity for existing and planned paths, including alternate separators, drive/UNC roots, case behavior, Unicode normalization, symlink/junction aliases, and fail-closed ambiguity.
- [x] 1.7 Apply the filesystem identity helper to scope checks, protected roots, artifacts, repository selectors, workspace registry claims, and collision detection with focused path tests.
- [x] 1.8 Make controller and hook protocol I/O explicitly UTF-8/LF, make subprocess capture byte-oriented with deterministic diagnostics, and distinguish spawn failures from child exit failures.
- [x] 1.9 Harden same-directory atomic replacement, temporary cleanup, and platform-appropriate durability while preserving the previously committed file on every failure.
- [x] 1.10 Enforce/test POSIX `0700` directory and `0600` controller-file modes plus standard-library Windows owner/DACL verification, rejecting null, unreadable, or broadly writable security descriptors before commit.
- [x] 1.11 Own, terminate, escalate, and reap mutating child processes before unlocking; persist/test a durable unready quarantine that blocks later mutation when quiescence cannot be proven.

## 2. Capability-Aware Git and Worktree Evidence

- [x] 2.1 Discover and verify effective `core.fileMode`, `core.symlinks`, `core.ignoreCase`, filesystem case behavior, and line-ending context without forcing incompatible Git settings.
- [x] 2.2 Bind an explicit evidence-contract version and verified capability-profile digest into preflight, repository fingerprints, baseline/workspace records, test evidence, and review snapshots with additive persisted fields.
- [x] 2.3 Preserve a deterministic raw/lossless tracked-entry manifest covering index mode/object/stage, worktree type, exact regular-file or symlink-target bytes, and initialized submodules.
- [x] 2.4 Retain and test fail-closed handling for hidden index flags, executable filters, hostile Git environment/config redirection, replacement objects, lazy fetch, dirty submodules, and case-colliding trees.
- [x] 2.5 Make status and manifest behavior respect native autocrlf/file-mode/symlink/case capabilities while still detecting every tracked-byte change.
- [x] 2.6 Harden repository selectors, branch/ref identity, worktree plan claims, pre-mutation revalidation, and postcondition proof for drive, UNC, long, case-equivalent, space, and Unicode paths.
- [x] 2.7 Add real-Git tests for clean and dirty capability profiles across preflight, analysis worktree, implementation worktree, fingerprint, test, and review flows.
- [x] 2.8 Add compatibility tests that preserve readable legacy schema-v1 state but invalidate/regenerate legacy evidence, reject newer unsupported evidence contracts, and prevent supported rollback guidance from resuming with a pre-profile build.

## 3. Portable Hook Launch and Guardrails

- [x] 3.1 Add a packaged Windows command shim that locates a supported Python launcher, preserves stdin/stdout/exit status, and invokes the same `dev_flow_hook.py` handler.
- [x] 3.2 Add paired `command` and `commandWindows` entries for every bundled hook while retaining the official canonical `Bash` matcher and default `hooks/hooks.json` discovery.
- [x] 3.3 Make hook bootstrap/checkpoint context preserve `sys.executable`, `PLUGIN_ROOT`, and explicit `PLUGIN_DATA`, with non-mutating diagnostics when required plugin environment is unavailable.
- [x] 3.4 Replace POSIX-only executable identification with slash-neutral basename and executable-suffix handling for direct and absolute `git`/`git.exe` invocations.
- [x] 3.5 Implement conservative wrapper-aware parsing for supported POSIX shells, `cmd.exe /c`, Windows PowerShell, and `pwsh -Command`, including chaining, quoting, escapes, and diagnostic denial of ambiguous recognized payloads.
- [x] 3.6 Add parameterized parser tests proving equivalent protected and benign Git decisions across executable spellings and every supported wrapper.
- [x] 3.7 Execute the actual packaged POSIX and Windows hook command entries in platform-appropriate tests with JSON events and plugin paths containing spaces, Unicode, and shell metacharacters.

## 4. Skills, Manifest, Package, and Documentation

- [x] 4.1 Update workflow skills and references so `<ctl>` always means the injected interpreter/controller/`--data-dir` prefix and platform-neutral examples contain no hard-coded `python3` or unlabeled POSIX continuation.
- [x] 4.2 Keep unsupported `hooks` out of `.codex-plugin/plugin.json`, run plugin-creator manifest validation independently, and add a separate standard-library default-hook/package check for `hooks/hooks.json`, runtime, skills, assets, templates, and portable case spelling.
- [x] 4.3 Remove stale references to unshipped `AGENTS.md`/template assets or intentionally add and validate the referenced files, then make inventory tests reject future missing or case-colliding paths.
- [ ] 4.4 Update `README.md` with the Windows/macOS/Linux support contract, Python 3.9–3.14 and Git prerequisites, native state/permission policies, evidence-version downgrade boundary, launcher behavior, validation, recovery, and shell-labelled examples, including exact commands to create the byte-preserving handoff on macOS/Linux and run it on Windows, how to provide local/UNC aliases of one test directory, the canonical digest and new report path, report return, and the distinction from publish/install authorization.
- [ ] 4.5 Bring `README.zh-CN.md` and `INSTALL.md` to semantic parity with the English contract, including the project-local Windows self-test and Windows Codex-host smoke as distinct procedures, and ensure every documented local path exists in the package.
- [ ] 4.6 After 5.8, 5.9, 5.10, 4.4, and 4.5 make every canonical-inventory implementation, workflow, and documentation input stable, use the plugin-creator cachebuster helper once, preserving the `0.2.0` base version and replacing rather than stacking any Codex suffix; tasks 6.1–6.5 and the returned Windows report must bind the resulting exact canonical candidate digest.

## 5. Portable Test Suite and Native CI

- [x] 5.1 Replace `true`, `cat`, `/bin/sh`, shebang-execution, and broad OS skips with `sys.executable`, standard-library helpers, and narrowly capability-gated assertions.
- [x] 5.2 Isolate repository, global, and system Git configuration in ordinary fixtures and retain explicit adversarial tests for hostile configuration.
- [ ] 5.3 Add native regression coverage for default directories, actor discovery, CRLF plus explicit legacy non-UTF-8 Windows code-page Unicode protocol round-trips, exact task-ID boundaries, path identity, atomic replacement, POSIX/Windows permissions, lock contention, child quiescence/quarantine, and interruption recovery; keep this task incomplete until the project-local runner returns a matching native Windows PASS for the code-page checks.
- [ ] 5.4 Add native regression coverage for packaged hook launch, shell wrappers, Git capability profiles, line endings, byte manifests, and real managed-worktree flows through accessible Windows drive, UNC-alias, and long paths, including postcondition and tracked-byte fingerprint proof; keep this task incomplete until the project-local runner returns a matching native Windows PASS for the path/worktree checks and cleanup.
- [x] 5.5 Add a required GitHub Actions matrix running Python 3.9 and 3.14 on Windows, macOS, and Linux plus Python 3.10–3.13 on Linux with real Git.
- [x] 5.6 Add an automated AST/import inventory and isolated-startup check that rejects and names any third-party import in shipped controller/hook runtime files.
- [x] 5.7 Make every required CI job run the full unittest suite, standard-library dependency audit, strict OpenSpec validation, independent manifest/default-hook/package checks, and all available bundled skill validators with actionable OS/Python diagnostics.
- [ ] 5.8 Add a shared standard-library canonical candidate-identity helper with exact v1 bytes (`b"dev-flow-canonical-v1\x00"`, unsigned 64-bit big-endian lengths/count, file tag `0x46`) and the normative two-entry SHA-256 vector, explicit allowlist/exclusions, collision/path/type rejection, `.gitattributes` line-ending policy, and dual canonical/host-local output in `scripts/run_bundled_validators.py`; add a deterministic outside-root exclusive-create ZIP/external-manifest builder, reject symlink/reparse/special entries, register every new file in package validation, and test the golden vector, path/type/byte drift, mode independence, checkout transformation, exclusions, archive reproducibility, and cross-OS digest parity.
- [ ] 5.9 Ship `scripts/windows_native_validation.py` plus `scripts/windows_native_validation.cmd` with verified handoff/archive/expected-canonical, safe member-set/path validation and binary extraction without `extractall`, explicit same-identity local/UNC roots, non-UTF-8 code page, outside-root new report, and safe cleanup options; preflight all inputs before mutation, create one nonce/sentinel-owned child, emit redacted stable JSON evidence without overwriting, add portable unit tests for CLI/report/digest/archive/alias/cleanup failure paths, and prove non-Windows execution cannot report native PASS.
- [ ] 5.10 Before candidate freeze, update `.github/workflows/cross-platform.yml` with a required `workflow_dispatch` reviewed-canonical input for authorized release runs, validate exact lowercase 64-hex format and presence, propagate it unchanged to every Windows/macOS/Linux matrix job, assert the canonical-v1 golden vector, fail every digest mismatch, retain ordinary push/PR checks without treating them as authorized release evidence, and test/validate the workflow contract; 6.5 must invoke this frozen workflow without source edits.

## 6. Verification and Release Evidence

Finalization order is mandatory: complete 5.8, 5.9, and 5.10, finish documentation tasks 4.4 and 4.5, perform 4.6 once every canonical-inventory input is stable, run 6.1 and 6.3 plus the pre-handoff portion of 6.4 and create the byte-preserving handoff against that exact canonical candidate, obtain the matching user-run Windows evidence for 5.3 and 5.4, update only OpenSpec progress, then run 6.2 and 6.4a before the separately authorized 6.5 and 6.6. Any canonical-inventory change invalidates 4.6, 6.1–6.5, the handoff, and the Windows report; OpenSpec progress-only changes require the post-report strict validation and host-local review but no second Windows run.

- [ ] 6.1 Run `python3 -m unittest discover -s tests -v` on the exact final cache-busted canonical candidate and resolve every failure without broadening platform skips.
- [ ] 6.2 After reviewing the matching Windows report and updating tasks 5.3/5.4, run strict validation for `complete-cross-platform-support`, prove the final canonical candidate is unchanged, and verify every proposal capability has one matching valid specification.
- [ ] 6.3 Run the automated standard-library audit, bundled skill-creator validator against every shipped skill, plugin-creator manifest validator, and project default-hook/package inventory validator against the same final canonical candidate digest.
- [ ] 6.4 Before handoff, inspect the full committed, staged, unstaged, ignored-generated, and untracked source for unintended files, runtime third-party imports, weakened gates, or documentation/implementation drift; record both the same canonical digest used by 6.1/6.3 and the separate mode-sensitive host-local full-snapshot digest, then create and independently verify the deterministic Windows ZIP/external-manifest pair without changing the candidate.
- [ ] 6.4a After the matching Windows report updates only OpenSpec progress and 6.2 passes, repeat the host-local full-snapshot review, prove the canonical digest is unchanged, and prove every post-handoff source difference is limited to the expected OpenSpec progress state.
- [ ] 6.5 After explicit publication authorization, invoke the already-frozen workflow without editing source, pass the approved canonical digest as its required immutable input, run the native Windows/macOS/Linux CI matrix against it, require every job to match, and record green job evidence before claiming native release verification.
- [ ] 6.6 After explicit Windows-host installation authorization and integration into a confirmed local marketplace source, use the plugin-creator update flow without hand-editing marketplace configuration, reinstall the same canonical candidate on an actual Windows Codex host from a path containing spaces, Unicode, `&`, and parentheses, start a new task, and record default hook discovery, `commandWindows`, real `PLUGIN_ROOT`/`PLUGIN_DATA`, bootstrap pickup, and benign/protected command results.
