## 1. Executor Cancellation Closure

- [x] 1.1 Extend `follow-dev-flow` with the source-confirmed repository-mismatch stop, exact-task authorization request, active-state disclosure, controller cancellation, failure handling, and terminal-projection verification steps.
- [x] 1.2 Update operator documentation for wrong-repository recovery without changing Hook, controller, immutable membership, or Git authority boundaries.

## 2. Candidate Enforcement

- [x] 2.1 Extend package validation to require both unauthorized and authorized repository-mismatch guidance, exact terminal fields, and the no-Git-mutation boundary.
- [x] 2.2 Add focused candidate-mutation tests that fail when either side of the cancellation handshake is omitted.

## 3. Verification

- [x] 3.1 Run strict OpenSpec validation for `fix-wrong-repository-cancellation` and inspect apply instructions against the completed plan.
- [ ] 3.2 Run the focused package, controller-cancellation, Hook, Git-read-only, and installed-journey tests that prove the modified guidance and unchanged runtime boundaries.
- [ ] 3.3 Validate every Skill with the bundled skill validator and validate the complete plugin candidate before handoff.
